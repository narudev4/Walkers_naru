# Authentication & Secrets
.env
.env.*
!.env.example
!.env.sample
# 例外: 実 API キーが含まれてるため除外（1Password 移行後にダミー化予定）
05_development/youtube/comment_automation/.env.example
.mcp.json
*.key
*.pem
credentials/
secrets/
**/credentials.json

# API Keys & Tokens
**/api_key*
**/token*
**/secret*

# Claude Code (personal settings)
.claude/settings.local.json

# OS Files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node.js
node_modules/

# Playwright MCP (自動生成のコンソールログ／aria tree スナップショット)
.playwright-mcp/

# Python キャッシュ
**/__pycache__/

# Remotion レンダリング成果物（ai-video-conte は再生成可能、out/ 配下の他ファイルは追跡継続）
05_development/remotion/out/ai-video-conte*.mp4

# shinkoku 確定申告データ（個人情報を含む — 削除しないこと）
shinkoku.config.yaml
shinkoku.db
shinkoku.db-wal
shinkoku.db-shm
.shinkoku/
output/

# YouTube 動画制作の作業ディレクトリ（projects/ 配下は全て track 外）
05_development/youtube/projects/

# PTJ AI健康相談PoC（nested 独立 git リポ・後で個人GitHubに切り出す）
03_projects/PTJ_パーソナルトレーナージャパン/app/

# Top-level image dumps (use 05_development/youtube/docs/screenshots/ or 03_projects/{slug}/ instead)
/*.png
/*.jpg
/*.jpeg
/*.gif
# Cross-device sync (rclone bisync state, locks, conflict copies, logs)
.sync.lock
.sync-logs/
.sync-daily/
.sync-gitleaks-report.json
*.conflict-*
**/*.conflict-*

# rclone config は credentials/ 配下に置く (credentials/ は既に除外済)
# 念のため top-level の rclone.conf も除外
/rclone.conf
**/rclone.conf

# Agent runtime のローカル開発用ファイル
05_development/agent-runtime/.env.local
05_development/agent-runtime/.workspace-mcp/
.vercel
.review/

# 独立 git リポジトリ（個別 GitHub で管理 or 参照用 clone）
# OKWEB 案件: 技術検証 + リプレース対象システムの参照用 clone
03_projects/OKWEB/aikata-auth-test/
03_projects/OKWEB/repos/
# mailmag 自動化: github.com/narudev4/Walkers_mailmag-automation
05_development/mailmag-automation/
