// @ts-check
/**
 * 【移行済み】このスクリプトは webhook/watch.js に置き換わりました。
 * 新版は Webhook サーバと同一パイプライン（③都度払いの自動送信・API検証込み）を通ります。
 *
 *   cd webhook && npm run watch
 */
console.error('このスクリプトは移行済みです。新版を使ってください:\n  cd webhook && npm run watch');
process.exit(1);
