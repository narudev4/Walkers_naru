# E-5 新カスタムアプリ作成 実施記録（2026-08-17）

実施: 細谷（Claude in Chrome で管理画面操作）。土台: `23-移管手順書_20260806.md` の【A-1】。
方針: **作成と切替を分離**。本日は「作成・スコープ付与・インストール」まで（本番系に影響なし）。トークン切替・Webhook 再登録・通し確認は別枠（8/18〜19 目安）で実施する。

## 作成したアプリ

| 項目 | 値 |
|---|---|
| アプリ名 | Ochiai Integrations (Production)。 |
| アプリ ID | 411439693825。 |
| URL | https://admin.shopify.com/store/xn-dfum9d9e7a6a1b3d8dc3778hkjvcsh3d/settings/apps/development/411439693825/overview |
| 作成 | 2026-08-17 05:18 UTC（naru.hosoya@walker-s.co.jp）。 |
| スコープ保存 | 2026-08-17 05:22 UTC。 |
| インストール | 2026-08-17 05:25 UTC（「アプリがインストールされました」トースト確認）。 |
| Admin API トークン | **未開示のまま温存**（1回のみ表示のため、切替当日に開示して即 Workers env へ投入する）。 |
| Webhook イベントバージョン | **2026-07 (最新)** のまま。旧アプリは 2026-04。下記【残課題1】参照。 |
| アプリ開発者欄 | DaikiFurutani (fullsrodd@gmail.com)（既定のまま）。下記【残課題3】参照。 |

## 付与スコープ（22件・設定タブの表示で確認済み）

旧アプリ「B2B Discount Tool (naru verification)」の20件（2026-08-17 に設定タブで実測・下記）＋ 追加2件。

- 旧アプリと同一（20件）: write_customers, read_customers, write_products, read_products, write_draft_orders, read_draft_orders, write_metaobjects, read_metaobjects, write_metaobject_definitions, read_metaobject_definitions, write_merchant_managed_fulfillment_orders, read_merchant_managed_fulfillment_orders, write_orders, read_orders, write_fulfillments, read_fulfillments, write_content, read_content, write_themes, read_themes。
- 追加（2件）: **write_shipping, read_shipping**（D-3 送料プロファイルの API 設定用。23【A-1】の 8/6 追記どおり）。

> 手順書の「旧アプリの16スコープ」は誤記。旧アプリの実付与は上記20件（工程表 A-8 備考の「21スコープ」とも食い違うが、管理画面の設定タブ表示では20件。実効スコープの最終確認は切替時に currentAppInstallation で行う）。

## 残課題（切替フェーズで対応）

1. **Webhook イベントバージョンが 2026-07 のまま**。ネイティブのセレクト UI が自動操作で変更できなかった。対応はどちらか: (a) naru が管理画面で手動で 2026-04 に変更（設定タブ > Webhook サブスクリプション。5秒で完了）。(b) 2026-07 のまま進め、切替の通し確認で orders/create ペイロードの互換を実測する。**推奨は (a)**（変数を1つ消せるため）。
2. **read_payment_gateways は付与不可**。レガシーカスタムアプリのスコープ一覧に存在しない（「payment」「gateway」で絞り込んでもゼロ。旧アプリにも未付与）。A-8 の 8/6 実測で「6/18 の権限エラーは別原因の可能性が高い」とされており、現行実装の必要スコープには含まれていないため実害なしの見込み。切替の通し確認で問題が出たら再調査する。
3. アプリ開発者欄（Shopify からの連絡先）が古谷さんの個人 Gmail のまま。動作には無関係。気になるなら管理画面の「アプリ設定」タブから変更できるか naru が確認する。

## 切替フェーズのチェックリスト（8/18〜19・約30分）

1. API 資格情報タブで「トークンを一度表示する」→ 即 Workers env（SHOPIFY 系トークン）へ差し替え。
2. **同時に SHOPIFY_WEBHOOK_SECRET も新アプリの API シークレットキーへ差し替える**（Webhook を新アプリで再登録すると HMAC 署名鍵が新アプリのシークレットに変わる。トークンだけ替えると受信が全部 401 になる）。
3. webhook-admin.js で orders/create 購読を新アプリで再登録（向き先 https://ochiai-integrations.ochiai-corp.workers.dev/api/webhook）。旧購読（ID 1922609545517）は切り戻し先としてそのまま残す。
4. 通し確認: テスト注文 → Webhook 受信 → Draft Order／請求書パイプライン（MF 実発行はしない）。
5. 異常時の切り戻し: env を旧トークン・旧シークレットへ戻し、購読を旧アプリへ再登録。旧アプリのトークン失効は移管後まで実施しない。

## 【2026-08-18 追記】切替フェーズ完了（前倒し実施・2026-07 で確定）

naru 判断「2026-04 に下げず、最新の 2026-07 のまま実測で通す」を採用し、8/18 未明に切替まで完了した。

1. トークン開示 → Workers シークレット差し替え（`SHOPIFY_ADMIN_TOKEN`・`SHOPIFY_WEBHOOK_SECRET` とも新アプリ値。wrangler secret put で投入済み）。
2. Webhook 購読を新アプリで再登録（新 ID `gid://shopify/WebhookSubscription/1929135980845`・orders/create → Workers）。旧購読 1922609545517 は削除（旧アプリとトークンは温存。切り戻しは env を旧値に戻して webhook-admin.js で再登録するだけ）。
3. **E2E 通し検証 PASS（#1034・prepaid）**: 新トークンで Draft #D68 作成 → 注文化 → **2026-07 ペイロードの Webhook を新 HMAC で検証・受信 → D1 記録 → MF 請求書下書き自動作成**まで全経路確認。テストデータは全削除済み（MF 204／D1 1行／注文・Draft 削除／デモ顧客復帰）。
4. これにより残課題1（Webhook バージョン）はクローズ。**E-5 の残りは「旧アプリのトークン失効」のみで、移管後に実施する（手順書 D）。**

## 経緯メモ

- スコープ選択 UI は「write_◯◯ をチェックすると read_◯◯ も自動選択」される仕様を実測（選択カウンタで 2→4→6→10→14→16→18→20→22 と各グループの増分を確認）。
- 「アプリをインストール」ボタンはスコープ保存直後は無効のままで、ページ再読み込みで有効化された。
