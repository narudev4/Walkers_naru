# F-10 注文確認メール — 分岐貼付と実注文検証（2026-08-17）

実施: 細谷（Claude in Chrome ＋ ローカルスクリプト）。土台: `11-メール文面案_20260803.md`（2026-08-10 本命確定版）。

## やったこと

1. **テンプレート貼付**: 管理画面 > 設定 > 通知 > お客様への通知 > 注文の確認 > コードを編集。`<p>{{ email_body }}</p>`（挨拶ブロック）の直後に、支払区分分岐ブロックを挿入して保存（「通知テンプレートが保存されました」トースト確認）。
   - 分岐: 本命 `attributes.payment_terms` ＋ 保険 注文 `tags`（`payment:prepaid`／`payment:20th`／`payment:eom`）の2段フォールバック。
   - 挿入範囲は `{%- comment -%} ▼ B2B支払区分ごとの案内 … ▲ ここまで {%- endcomment -%}` のコメントで明示（差し戻すときはこの範囲を削除するだけ）。
   - **振込先口座は仮**: 「【仮】◯◯銀行 ◯◯支店 普通 0000000 カ)オチアイコーポレーション」。回収後にこの1行のみ差し替える。
2. **都度払い（prepaid）の実注文検証**: デモ法人A（naru.hosoya+shopifydemo@walker-s.co.jp）を `prepaid` に切替 → **本番 Workers 経由**（POST /api/create-draft-order）で Draft #D67 作成 → draftOrderComplete で注文 **#1033** 化 → 受信メールを Gmail で実測。

## 結果（すべて PASS）

| 確認点 | 結果 |
|---|---|
| 件名・本文の言語 | **日本語**（件名「ご注文内容の確認　注文番号：#1033」・本文もすべて日本語）。 |
| 都度払いブロックの発火 | 表示された（「お支払いについて」・お支払い金額 ¥76,230・仮口座・注意書き3点）。 |
| 属性・タグの引き継ぎ | 注文に `payment_terms=prepaid` 属性＋`payment:prepaid` タグの両方を確認。 |
| 掛け率（新経路） | 区分C 適用で 7,700→6,930円（税込総額 76,230円）。「御社価格適用」表記。 |
| MF 連携（新経路の通し） | webhook 受信 → **MF 請求書下書き #19 を自動作成**（D1 に mf_invoice_id 記録）。**8/6 に宣言した「B-12 後の新経路での通し検証1件」はこれで消化**。 |

> 補足: 英語メール問題は今回の経路では再現しなかった（テンプレは店舗生成時から日本語ハードコード）。naru が見た英語メールは別ロケール・別経路由来の可能性が高い。テンプレをカスタマイズ保存した現在は、この日本語テンプレが常に使われる想定。英語が再発した場合はその注文の customer_locale を確認する。

## クリーンアップ（すべて完了）

- MF 請求書下書き #19（YEpMg8eVWgHjjcUphWkPKA）を API で削除（204）。
- D1 processed_orders の #1033 行を削除（changes=1）。
- Shopify 注文 #1033 をキャンセル（通知なし）→ 削除。Draft #D67 も削除。
- デモ法人Aの payment_terms を 20th に復帰。

## 残作業

1. **受信検証の残り2パターン**: かけ払い（20th or eom）と、一般のお客様向け（B2C・分岐なしで既定文面のまま）の確認。手順は本ファイルと同じ（B2C は B2C 顧客での通常注文）。
2. **振込先口座の差し替え**（先方回収待ち・仮口座の1行のみ）。
3. 発送通知ほか他テンプレートの文面確認（デフォルトが日本語のため、内容確認が主）。6桁コードメールは文面カスタム不可（Shopify 仕様）。
