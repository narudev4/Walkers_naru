# B-11〜B-15 実施記録（2026-08-05）

掛け率の読み先を古谷さん設計へ付け替える一連の作業記録。仕様は launch-plan/20、実体調査は launch-plan/21。

## B-11 安全準備 ✅

| 項目 | 値 |
|---|---|
| バックアップブランチ | `backup/pre-b12-20260805`（ochiai-theme・ochiai-integrations 両リポジトリ、push 済み） |
| 付け替え前 HEAD | theme `9ab759c`（+リモート先行 `ada9f86`）／integrations `7cb01cd` |
| 公開テーマ | ochiai-theme/dev（gid://shopify/OnlineStoreTheme/182102950189・MAIN） |
| Workers 旧デプロイ | 付け替え前の本番。ロールバック＝backup ブランチを wrangler deploy |
| ロールバック手順 | テーマ: `git push origin backup/pre-b12-20260805:dev --force` ／ Workers: backup ブランチで `npx wrangler deploy` |

## B-12 実装 ✅

- **テーマ**: 共有スニペット `snippets/ochiai-b2b-keep-pct.liquid` を新設（keep%＝100−割引率を出力）。b2b-wholesale-price／b2b-card-price／ochiai-order-row／ochiai-cart-b2b-line／b2b-cart-checkout／main-account／b2b-order-form（デッド）を付け替え。テーマ内の旧系統参照（discount_matrix・custom_overrides・discount_group）は**ゼロ**。commit `1929a79`。
- **Workers**: `createDraftOrder.js` — customer は `custom.2_f` を reference 展開＋`ovrA/B/C` alias、product は `custom.itemType` を reference 展開。`percentToRate`（(100−%)÷100・0は有効・負/100以上/非数は null）。appliedDiscount タイトルを「御社価格適用」に変更（掛率数値の露出を解消・B-7 の懸念）。commit `d1abba6`、デプロイ Version `8766acc8-f85d-4547-b6fb-4e7ac74b750b`。
- **参照実装**: `shopify/src/discountEngine.js` に `percentToRate`／`resolveRateFurutani` を追加。テスト 25 件（既存15＋新規10）全パス。Workers 既存テスト 86 件全パス。

## B-13 検証 ✅（実ストアで Draft Order 実測）

検証データ: ELITE TOP KS-301→商品区分A／KR-450→B／シューズK-086→C（仮割り当て・実割り当ては No.45④回答後に上書き）。TC-201 は古谷さんが既に A を設定済み。デモ法人B→区分A、テスト法人太郎→区分カスタム＋商品A個別25%（いずれも検証用・記録済み）。

| ケース | 商品区分A(7,700) | B(12,800) | C(7,920) | 区分なし(2,800) |
|---|---|---|---|---|
| 区分C（法人A・残留個別値あり） | ✓6,930（10%引） | ✓12,160（5%引） | ✓7,762（2%引・丸め一致） | ✓2,800（正価） |
| 区分A（法人B） | ✓5,390（30%引） | ✓10,240（20%引） | ✓7,128（10%引） | — |
| カスタム（太郎・A=25のみ） | ✓5,775（25%引） | ✓12,800（**未設定→正価フォールバック**） | — | — |

- 区分C顧客の残留個別値（90/50/40）が**無視される**ことを実測（8/4 古谷さん明言どおり）。
- 検証 Draft Order（#D46〜#D51）は全て削除済み。
- デプロイ後スモーク: 本番エンドポイントで 法人A×商品区分A×10個 → total 76,230円（=6,930×10）PASS。#D52 削除済み。
- ⚠️ 未実施: ストアフロント表示の目視（顧客ログインが必要。ロジックは共有スニペットで Draft Order と同一のため数値は一致するはず。次回 MTG の画面共有か、デモ顧客ログインで確認する）。検証用テーマ経由ではなく本番テーマ直反映とした（パスワード保護中の開発ストアでバックアップ即戻し可能なため。工程表 B-11 の記載からの逸脱として明記）。

## B-14 残留値クリア ✅

- 全顧客走査 → 残留はデモ法人Aのみ（itemA=90 / itemB=50 / itemC=40。metafield ID 43936228507949／43936228475181／43936228442413 — 復元用記録）。削除済み。
- クリア前後で Draft Order 価格が不変であることを再実測（6,930／12,160／7,762）。
- 定義の重複は無かったため統合作業なし（B-10）。「顧客区分2(f)」の改名は古谷さん確認後（任意）。

## B-15 公開反映 ✅・報告（文面案あり・送信は naru）

- テーマ push（`ada9f86` に rebase → `1929a79`）・Workers デプロイ済み。
- 古谷さんへの確認3点（公開ゲート）: ①30/20/10 はデモ値か実値か（No.45） ②商品区分A/B/C への実割り当ての担当 ③区分・商品区分未設定時の運用（現状は正価フォールバック）。

## 残タスク（B外）

- ストアフロント表示の目視確認（上記⚠️）。
- 質問事項タブへの ①〜③ の昇格（No.45 に追記 or 新規行）。
- 旧系統（discount_matrix・b2b.discount_group/custom_overrides/category）の削除判断は 9/1 試験運用後（B-11 の凍結方針どおり）。

## 追記: 壊れ値のエラー処理実地テスト（2026-08-05・naru 依頼）

検証用のカスタム区分顧客（エラー検証 検証用カスタム法人）を一時作成し、本番エンドポイント経由で商品区分A（7,700円）に対して実測。終了後に顧客・Draft Order とも削除済み。

| 個別割引率 | 期待 | 実測 | 判定 |
|---|---|---|---|
| 50%（正常値） | 3,850円 | 3,850円 | ✓ |
| 100%（無料化する壊れ値） | 正価 7,700円 | 7,700円 | ✓ フォールバック動作 |
| 150%（マイナス価格化する壊れ値） | 正価 7,700円 | 7,700円 | ✓ フォールバック動作 |

- 表示側（テーマ共有スニペット）も同一の判定（0 < 割引率 < 100 のみ有効）のため挙動は一致する。
- ✅ 2026-08-05 入力制限を実装（naru 承認）: 割引率の6フィールド（顧客の custom.itemA/B/CdiscountRate と customerclass の typeA/B/C）に**最小0・最大99**の検証ルールを後付け（定義の作り直し不要・既存値無傷）。「150」の書き込みが保存時点で「Value has a maximum of 99」と弾かれることを実証。守りは二段: ①入力時に定義制限で弾く → ②すり抜けても計算時に正価フォールバック。
