PORT=8788
# none: 下書き作成まで / browser: ③都度払いをPlaywright送信まで自動実行
SEND_MODE=none
# ③都度払いとして自動送信する payment_terms 値（カンマ区切り）
IMMEDIATE_TERMS=prepaid,immediate
# ローカルテスト: sign-and-post.js と同じ値なら任意。本番(API登録Webhook): カスタムアプリの「APIシークレットキー」
SHOPIFY_WEBHOOK_SECRET=replace-me
