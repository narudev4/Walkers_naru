# 建物名未取得 原因調査（NA-20260818-006）2026-08-18

> サブエージェント調査結果の保存。読み取りのみで調査（Airtable GET 約30回、dynamodb list-tables 1回、Gmail/Sheets 読取）。

## 結論
- **確定**: チタン API は 2026-06-16 時点で `物件名` キーを返している（48/50 件に値あり。岩下様が追加費用 8 万円で依頼した項目）が、**Airtable 側の取得スクリプト（v2.4〜v2.9 全版・sento 旧版）が `物件名` を Airtable「建物名」に一度も書いていない**。
- **実測**: 8/11〜8/18 登録の最新 500 件で建物名 **0/500（欠落率 100%）**。建物名が入っている最新レコードは 2026-05-15 登録が最後。
- 過去に入っている建物名は API 経由ではなく別経路（手作業・岩下様側ツール等と推測）。要確認。
- 現時点の API レスポンスに `物件名` が含まれることの直接確認は未実施（GET 1 回で確認可能）。

## 対応表
| 段階 | 項目 | 状態 | 根拠 |
|---|---|---|---|
| チタン API `json_output.php` | `物件名` | 6/16 確認で 48/50 件に値あり | マスターシート「API要求」Row2/Row30（`162dDCtBFHFNu-8M4q_3w5Gs528Kgg23aelQkko4zzDc`）、6/30 定例議事録 |
| Airtable 取得スクリプト（Automation `wflqREpmCKeVl0oQz`） | マッピング無し | `fields` に `物件名`/`建物名` が無い | `監査_automation_20260725/01_エリア別物件取得_v2.9.js:179-192`、旧版 `automation_script_DEPLOYED_backup_20260724.js:121` 付近、sento 版 `backupcode.md:209-221` |
| Airtable「チタン物件一覧」 | フィールド「建物名」 | 存在するが Automation 6 本のどれも書かない | `.playwright-mcp/page-2026-07-24T11-03-51-133Z.yml:386` |
| AWS batch | `display.name` ← 建物名。空なら「{所在地}の{種別}」 | フォールバックで欠落が目立たない | `05_development/1lc-property/batch/lib/airtable.mjs:57`, `engine.mjs:62`, `postmark.mjs:35-40` |
| AWS Web | `buildingName` ← 建物名。空なら h1 非表示 | `/admin/sync` は「—」 | `lib/airtable.ts:119`, `app/property/[pid]/page.tsx:132-133`, `app/admin/sync/page.tsx:134` |
| DynamoDB | 物件マスタ無し（queue/events/inquiries/links/settings のみ） | 物件は毎回 Airtable から読む | `infra/batch-delivery.yaml:48-61` |

## 月別サンプル（各月先頭100件の建物名あり率）
2025-03 31% / 04 23% / 05 26% / 06 18% / **07 85%** / **08 81%** / 09 17% / 10 6% / 11 5% / 12 3% / 2026-01 6% / 02 0% / 03 2% / 04 1% / 05 0% / 06〜08 0%。
建物名ありレコードは元付会社名・売主も埋まっている率が高い（別経路付与と整合）。

## 仮説の判定
- (b) 取得スクリプト未マッピング → **確定**。
- (a) チタン API 側で空 → ほぼ否定（現レスポンス未実測）。
- (c) 土地・戸建は本来無い → 主因でない（一棟マンション/アパートでも 0%。6/16 サンプルでは戸建でも「所在地＋戸建」の形で値あり）。
- (d) v2.9 デグレ → 否定。(e) 50件リミット/欠落3,200件 → 否定。(f) 表示側の参照違い → 否定。

## 次アクション案
1. 取得スクリプト `fields` に `"建物名": d["物件名"] || null` を追加（v2.10）。以後の update で自動的に埋まる。
2. `inai=2` のため掲載中 約 12,000 件のバックフィルは別途一括処理（`backlog_recover_*.py` と同型）。
3. **着手前に岩下様へ方針確認**: 6/4 定例で「建物名は顧客特定リスクで送信は控える。データは過去送付判定用に持ちたい」発言あり。現行メール（`name_display`）と物件ページ h1 は建物名があれば**そのまま表示**するため、埋めた瞬間に露出する。「取得のみ／表示も可」を先に決めてもらう。
4. 8/3 管理画面 QA #28（`/admin/sync` 物件名 89/100 が「—」）は同根。

## チタン様への質問文案
> いつもお世話になっております。株式会社Walkersの細谷です。
> 昨年ご対応いただいた JSON 出力の「物件名」項目について 2 点確認させてください。
> ①現在も `json_output.php` のレスポンスに「物件名」キーが含まれている認識で合っておりますでしょうか。
> ②レインズ側に建物名の登録が無い物件の場合、「物件名」には空文字が入るのか、所在地＋種別などの代替文字列が入るのか、仕様をお教えいただけますと幸いです。
> なお、こちらの取込スクリプト側で「物件名」を Airtable に反映していなかったことが判明したため、Walkers 側で修正を進めております。ご確認のほどよろしくお願いいたします。

## 未確認事項
- 現時点のチタン API レスポンスに `物件名` が含まれること（6/30 メールのサンプル URL に GET 1 回で確認可）。
- Airtable の過去建物名の出所（岩下様に確認）。
- 「物件名」が実建物名か生成値（所在地＋種別）か。
- Airtable スキーマ API は PAT スコープ不足で 403。

## 安全メモ
- Airtable PAT: `credentials/airtable-1lc-pat.txt`。
- 岩下様⇔チタン様メールスレッド（Gmail thread `19efd732af015945`）に FTP ログイン情報が平文で含まれる。転記注意。
