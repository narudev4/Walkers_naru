# マスターシートに載っていない差分（2026-07-30）

真理源は **1LC様マスターシート**
`https://docs.google.com/spreadsheets/d/162dDCtBFHFNu-8M4q_3w5Gs528Kgg23aelQkko4zzDc/`
（工程表 / システム稼働情報 / システム周り決定事項 / 土地大臣リプレイス：バックエンド処理検討）

本ファイルは 2026-07-25〜07-29 のマッチング監査で判明し、**まだシートに反映されていない事項**のみを記録する。
確定したものはシートへ昇格させ、本ファイルからは消す。

---

## 1. シートの記載に誤りがあるもの

| 場所 | 現在の記載 | 正 | 根拠 |
|---|---|---|---|
| システム周り決定事項 Row25「独自ドメイン」 | `bukken.1lc.com 等` | **`1lcinc.com`** のサブドメイン | Airtableの公開用URL 25,732件すべてが `www.1lcinc.com`。`1lc.com` は1LC様のドメインではない |

## 2. サブドメイン割当の前提（シートに未記載の調査結果）

シートは「ドメインをどれにするか。ACM証明書とDNS切替が必要」までしか書いていない。
DNSの管理者が誰かが未調査だったため、2026-07-30 に確認した。

| 項目 | 実態 |
|---|---|
| ドメイン登録＋**DNS（ネームサーバー）** | **PSI-Japan株式会社**（psi.jp / `b202〜204.psi-japan.net`） |
| Webサーバーの所在 | **DIX株式会社**（153.122.205.27） |
| ドメイン有効期限 | **2026-09-10**（自動更新か要確認） |

**依頼の仕方に注意**: ホスティング業者の管理画面で「サブドメイン追加」を実行すると
そのサーバー内にディレクトリが作られ、DIXのサーバーを指してしまう。必要なのはAWSを指すCNAME。

依頼形態の候補（望ましい順）:
1. PSI-Japanのドメイン管理画面にWalkers用アカウントを発行してもらう
2. **サブドメインのみ Route 53 に委任**（NSレコード1本の依頼で完結・`www`本体に影響なし）
3. 値が決まるたび業者へ都度依頼（ACM検証で往復が発生するため構築が停滞する）

将来SESへ切り替える際も同じDNSに SPF/DKIM/DMARC を追加するため、窓口は1回で確定させたい。

## 3. マッチングの不具合（工程表に項目が無い）

工程表のマッチング関連は Row44「マッチングロジックの確認（岩下様・未着手・動線改善後）」と
Row45「新マッチングロジックの再設計（低・後工程）」のみ。以下は**実害が出ていた事実**で、
優先度の再評価が必要。

| # | 事実 | 状態 |
|---|---|---|
| M-1 | 記録に残る **178回すべて "Failed to run"、成功0回**（最古 2026-02-03）。原因は180秒の実行時間上限。1人が超過するとAutomation全体が停止し、**以降の投資家全員に1通も届かない**。実測で紹介許可ON 322人中 26〜44%にしか届いていなかった | v3.1 / v3.2 で緩和（地雷 102人→6人・1人100秒で打ち切り）。**180秒の壁自体は残存** |
| M-2 | Airtableの物件の **53.5%（13,779件）が掲載終了（成約済）**。配信物件の43.1%が掲載終了だった | 「掲載終了」「掲載確認日」フィールドを追加しフラグ書き込み済。マッチングから除外・新しい順に変更（v3.2） |
| M-3 | **掲載状況チェックの仕組みが存在しない。** 2026-07-25に手動で1回実行したのみ。7/30に300件抜き取りで再確認したところ**5日間で7.4%が掲載終了化**（≒1日1.5%）。全体で約885件がフラグ未反映 | **未着手。日次自動化が必要** |
| M-4 | 築年数の不等号が逆（`age > years`）。フォームは「◯年以内」なので **152人が希望と逆の（より古い）物件を受信**。「下限無し」(9999)の81人・空欄17人は**1件も受信できていなかった** | 9999/-1/空欄は「条件なし」として v3.2 で解消。**数値指定時の向きは岩下様の確認待ち**（新規登録フォームの保存値が未確認） |
| M-5 | マッチングは**5件**取得するがメールは**3件**表示（v2/v3どちらのデザインも3件）。**表示されない2件も履歴に「紹介済み」として記録**され、二度と出ない。7/25実測で85人中78人がちょうど5件記録＝**1日156件が誰にも見られず消えている** | **未修正。3件に揃える方針（naru判断・2026-07-30）** |
| M-6 | マッチング条件のうち「積算価格の妥当性」「土地比率」「建物比率」は未実装、「バス・徒歩」は値を読むだけで条件式で未使用 | 岩下様に契約上の約束事項か確認 |

### 参考: 移行後の処理速度（2026-07-30 実測）

掲載中 11,958件 × 紹介許可ON 322人の全マッチングを手元のPythonで実行 → **0.10秒**。
Airtableは1人あたり3〜5分かかり180秒で落ちている。制約はAirtable固有のもの。

## 4. 池田さんの作業との重複リスク

工程表 Row29「Airtable利回りフィールドの欠落修正（池田・進行中）」のメモに
**「マッチング不全の原因。ほぼ特定済」**とある。

一方で M-1（180秒タイムアウトによる178回連続失敗）は独立した別原因。
**同じ「マッチング不全」を別の原因で追っている可能性があるため、M-1〜M-5 を池田さんへ共有する必要がある。**

## 5. 未反映の関連事項

- **Postmarkトークン**: システム周り決定事項 Row24 は「（解消済）」としているが、これは
  *記録した*という意味。**本番のAutomationスクリプトには平文のまま残っており、ローテーション未実施**
- **B-17（テンプレート変数確認）**: `href="{{url}}"` の形式であることは確認済。ただし
  **subs を何件描画するか（3件固定か可変か）は未確認**。M-5 の修正方針に影響する
