# 画面テスト所見の仕分け表（叩き台）— 2026-08-03

## 2026-08-12 現状確認（新環境 d1j66qnsj1enpq のコード・APIを実測）

C2移設後の新環境と最新コード（full999/1lc-property main）で「直す」上位項目の消化状況を確認した。

| 項目 | 状態 |
|---|---|
| #1 ヴィラ写真（imageUrl null → 全物件同一のUnsplashストック写真) | **✅ 2026-08-17 修正・本番反映確認済み**（placeholder.svg へ差し替え。unsplash参照が本番から消えたことを実測確認） |
| #1.5 CV率の分母がUU（50%/100%表示） | **✅ 2026-08-17 修正**（分母を総閲覧数に変更。commit a2fcd21） |
| （8/4定例）F物件の画像非表示 | **✅ 2026-08-17 暫定対応・本番反映確認済み**（F物件の imageUrl を null 化。チタン帯上書き完了で外す。API実測で F物件の画像露出0件） |
| #1.6 問い合わせ種別4択の未保存 | **✅ 修正済み**。モーダルが `document_request`/`price_negotiation`/`inquiry` を送信し、管理画面にラベル表示あり。「内覧の申し込み」は 8/4 naru 指示で選択肢ごと削除 |
| #6 トップ「21,000件超」「更新日時6/30」の乖離 | **✅ 修正済み**（8/3 コミット: active-only件数・ハードコード時刻削除） |
| #7 価格「90000万円」表記 | **✅ 修正済み**（APIが `priceDisplay: "19,000万円"` を返す） |
| #33関連 テスト問い合わせの残存 | **新環境は0件**（引き継がれず）。旧環境 d2x74qj3vtjvpl に「テスト送信（Walkers細谷）」1件が残存。旧環境は移設完了後に廃止予定のため、旧環境廃止までに実問い合わせが旧に来ない前提なら放置可 |

その他: 8/3以降のリポジトリ変更は配信パイプライン系（バッチ配信・配信履歴のAirtable記録・売止め除外・掲載ステータス日次スイープLambda）＋上記修正のみ。**#2 最終同期表示・#4 mypage壊れ画像・#5 ページネーション等は未着手**（コード上の修正コミットなし）。

### 計測・配信の数値の現状（2026-08-12 実測）

- **配信side（送った数）は取れている**が置き場は AWS 側（send Lambda ログ・Airtable履歴・DynamoDBキュー）。8/5 初回 sent=184/failed=30 で稼働継続中。**管理画面KPIの「配信数」への連携は未実装のまま**（既知課題のまま残存）。
- **メール→サイトの閲覧計測はゼロ**: 計測イベントは新環境0件・旧環境30件（ほぼ全部8/3QA分、linked uid はテスト1件のみ）。実問い合わせも0件。
- **原因は設計どおりの「未接続」**: 配信メールの物件リンクは Airtable「公開用URL」＝チタン提供の既存URLのままで、新サイトに誘導していない。ロードマップ2-3で「**ドメイン確定までは差し替えない**（amplifyapp.com を載せるとドメイン切替時にリンクとCookieが全部切れる）」と明記済み。
- **前提のドメインが未委任**: bukken.1lcinc.com の NS委任（8/7 に岩下様→管理会社へ依頼）は 8/12 時点で dig 応答なし＝**まだ委任されていない**。数営業日経過しており督促検討ライン。
- 計測開始の順序: NS委任 → 新Amplifyへドメイン紐付け → メールリンク差し替え（2-3）＋uid付与方式（/p/トークン or Cookie、リンク発行の設計見直し#10と同時）→ ここで初めて「誰がどの物件を見たか」が動く。
- F物件の画像非表示（8/4定例NA・@AI担当）が**未実装**: F物件30件中24件が c21.to 画像を露出中（他社名写り込みリスク）。

> 47（チェックリスト）・48（管理画面QA）・49（公開画面QA）の全所見を「直す / そのまま / 岩下様に聞く」に仕分けた細谷の叩き台。**分類は naru の確認・修正待ち**。「直す」の実施はテストとは別作業（今日はコード・AWSに触らない約束のため、着手タイミングは naru 判断）。

## 直す（Walkers側で修正。優先度順）

| # | 所見 | 補足 |
|---|---|---|
| 1 | 詳細ページのストック写真: 写真null物件58件に同一のプール付きヴィラ画像 | 最優先。実物件と誤認させる表示で景表法・宅建業法の観点でも危険。最低でも placeholder.svg に統一。**明日のデモ前に直すか、デモで踏まない運用にするか要決定** |
| 1.5 | CV率の分母がユニーク閲覧者で「50.0%」「100.0%」等のありえない数字が出る | デモで「成約率50%」と誤読されるリスク。分母を閲覧数にするか、母数が小さいうちは非表示に |
| 1.6 | 問い合わせ種別の4択（資料請求/内覧/価格交渉/その他）が保存されず、管理画面で用件が分からない | 保存値は常に `type: "inquiry"`。フォームで選ばせた情報が捨てられている |
| 2 | `/admin/sync` の「最終同期」が同期時刻でなく画面を開いた時刻 | 常に「たった今」で情報量ゼロ |
| 3 | KPIダッシュボード初回アクセスで全数値0表示 | ローディング中に「0件」を出す実装。**当面はデモ前に一度開いて温める運用で回避** |
| 4 | mypage のカードだけ画像フォールバック無しで壊れ画像がそのまま出る | 他画面は placeholder に落ちる |
| 5 | `/properties` にページネーションが無く後半50件に到達不能 | データ100件に対しカード50件 |
| 6 | トップの「21,000件超」「更新日時 2026/06/30」が実データ（100件・当日）と乖離 | 管理画面との数字矛盾もこれが原因。実数連動 or 文言変更 |
| 7 | 価格表記「90000万円」 | 桁区切り or 億表記に |
| 8 | `/admin/properties` の物件名と所在地の同一文字列二重表示 | |
| 9 | 物件詳細の「建物比率 %」が値空のまま単位だけ表示 | |
| 10 | `/admin/links` の発行単位が「投資家100名×物件」一括で個別選択・削除UI無し | 実運用前に発行単位の設計見直し。選択可能物件が50件のみの件も同時に調査 |
| 11 | `/api/inquiries?uid=` が uid だけで問い合わせ履歴を返す（疑い） | 既知の `/api/investor/{uid}` 封鎖対応（細谷担当）に合流して確認・封鎖 |
| 12 | 同期・画像アイコン（緑✓/黄△）に凡例なし | 小 |
| 13 | `/admin/properties` の件数ラベルがフィルタに追従しない（4件表示中も「100件」のまま） | 小。※問い合わせ側の空状態メッセージは正常動作と判明したため対象から除外 |
| 13.5 | 問い合わせ一覧に物件名が出ず問合せ番号のみ（APIに物件名フィールド自体が無い） | 岩下様が番号では物件を特定できない |
| 13.6 | 滞在時間の表記が画面間で不統一（「3分10秒」と「3:24」） | 小 |
| 14 | KPIの開発者向け文言（「チタン物件一覧(取得上限200件)」「uid単位」） | 小。岩下様向けの言葉に |
| 15 | 管理画面→公開ページに出た後、管理画面へ戻る導線が無い | 小 |
| 16 | `/admin/investors` の並び順がレコードID昇順 | 小。登録日 or 五十音に |

## そのまま（現状維持。デモでの扱いだけ注意)

| # | 所見 | デモでの扱い |
|---|---|---|
| 17 | `/admin/investors` 全員ゼロ | **naru決定済み**: ゼロのままデモ（以前画面を見せているため）。QA用リンク発行はしない |
| 18 | 物件別パフォーマンスの計測指標 | **全0は解消済み**（公開画面テストのアクセスで11物件に実データ。最大 閲覧6件・滞在14分29秒・倉敷は問合せ1）。実データを見せながら「配信開始後に本格的に埋まる」と説明できる。ただし同じ行に #1.5 の CV率100% が出るので見せ方に注意 |
| 19 | 通知設定が「有効」表示なのに通知先未設定 | Postmark連携・通知先設定はローンチ準備で実施。デモで「通知が届く」とは言わない |
| 20 | 実名・実メール100件が `/admin/investors` に表示 | 画面共有時の映り込みに注意 |
| 21 | 「再同期」ボタンが常時露出 | デモ中に誤クリックしない |
| 22 | 生uid（rec...）がカードに表示 | 実害なし。将来の磨き込み |
| 23 | 検索条件がURLに入らない（共有・ブックマーク不可） | ローンチ後の改善候補 |
| 24 | ユニーク閲覧者が匿名を1人扱い | uid消失のCookie方式解決（設計済み・既知）に含まれる |
| 25 | 初回アクセスの `GET /?_rsc` 401 | RSCプリフェッチのみでユーザー影響なし |

## 岩下様に聞く（データ・方針の確認が必要）

| # | 所見 | 聞くこと |
|---|---|---|
| 26 | 物件写真が正しく出るのは100件中19件（null58・404 21・電話バナー2） | 画像は外部 `c21.to`（Century21）直リンク依存で、元サイト側で消えている可能性が高い。**画像の取得・保存方針**（自前ストレージに複製するか等）と、チタン側への確認可否 |
| 27 | 重複掲載: 鹿児島市桜ヶ丘５丁目が別IDで2件（F3859062014 / R3559671461） | 元データ由来。どちらが正か、重複排除のルール |
| 28 | 利回り空16件・不自然値（0.6%等）、`/admin/sync` の物件名89件が「—」 | 元データの品質。表示側で隠すか、データ側で直すか |
| 29 | 「誰がどの物件を見たか」の物件名が `/admin/investors` に出ない | デモの売り文句との整合。データが入れば出る設計かの確認は Walkers 側で先に実施 |
| 30 | トップのヒーロー画像がUnsplashストック写真 | 会社として使いたい素材（自社写真・鹿児島の画像等）があるか |
| 31 | 検索の価格帯・エリア等の選択肢構成 | 岩下様の顧客層に合った区分か（デモで一緒に確認） |

## 保留（検証未了）

| # | 項目 | 状態 |
|---|---|---|
| 32 | スマホ幅390pxの崩れ・横スクロール・文字サイズ | resize_window がバックグラウンドタブに効かず未検証。**naru が DevTools デバイスモードで確認** |
| 33 | ~~問い合わせ1件入り状態の `/admin/inquiries` 表示~~ | **検証完了・✅正常**。氏名・メール・本文・時刻（18:53:55）・ステータス（未対応）とも送信値と完全一致、二重登録なし、電話空欄は項目ごと非表示で崩れなし。ただし種別未保存（#1.6）と物件名なし（#13.5）を新規発見 |
| 34 | mypage の実データ表示 | 有効な uid が管理画面UIから取得できず未検証。#11 の封鎖対応時に合わせて確認 |
| 35 | 配信リンクの実URL形式・`/p/トークン` の動作 | 未発行のため未検証。発行単位の設計見直し（#10）後に実施 |

## 既知課題（このテストの対象外・対応方針決定済み）

- `/login` は見た目だけ（消す方針で判断待ち）。
- uid がページ間移動で消える（Cookie方式で解決する設計済み）。
- `/api/investor/{uid}` の個人情報応答（ローンチ前封鎖・細谷対応）。
- KPI配信数の空欄（Postmark連携未実装）。
- 問い合わせステータス変更UIなし。
