# 配信メールへの物件ページリンク掲載 調査（NA-20260818-002）2026-08-18

> サブエージェント調査結果の保存（読み取り専用・書き込みゼロ）。リポ: `05_development/1lc-property`（feat/aws-batch-delivery）。

## 結論
- 本番 Postmark テンプレート `lc-v3-prod`(45535824) には既に `href="{{url}}"` が hero ボタン（66行目「物件の詳細を見る ▶」）と subs（97行目「詳細を見る ▶」）にある。今は `url` に土地大臣 URL。
- **フェーズ 1 はテンプレート無変更で、send Lambda が `url` に入れる値を `https://bukken.1lcinc.com/property/{問合わせ番号}?uid={投資家recId}&src=email` に差し替えるだけ**。
- pid（`問合わせ番号`）はキュー項目に無いが `url` の `bukken_XXXX_` セグメントと 1:1（本日キュー 216 件・592 物件で抽出 0 失敗、Airtable 12/12 一致）。
- **顧客への本番反映は `SITE_BASIC_AUTH`（未公開ゲート）が掛かっている限り不可**（`/property/F3836835978` は 401 realm "1LC Preview"）。ゲート解除 or 部分開放の意思決定が前提。

## 事実
- 組み立て: `batch/handler-send.mjs:47-97` → `sendMatchEmail`。`TEMPLATE_ID=45535824`・`FROM`・`ENABLE_IMAGES=false` は定数 `batch/lib/postmark.mjs:8-10,34`。TemplateModel `str_name/edit_link/conditions_summary/hero/subs` `postmark.mjs:72-78`。`url` は `engine.mjs:58`（Airtable `公開用URL`）→ `postmark.mjs:59` → `handler-match.mjs:58-76`。
- env: `QUEUE_TABLE / DELIVERY_WHITELIST / POSTMARK_SECRET_ARN / AIRTABLE_SECRET_ARN / HISTORY_WRITE`（`infra/batch-delivery.yaml:128-134`）。実機 WHITELIST=`*`。
- testTo モード `{testTo, investorId}`: キュー status は触らない `handler-send.mjs:25-44`。
- 配備 Lambda zip は feat HEAD (d7b7f64) の `batch/` と全ファイル一致。反映は S3 + `update-function-code` 直接（スタック更新は 8/4 のまま）。
- 詳細ページ `/property/{pid}`、pid=Airtable `問合わせ番号`（`lib/airtable.ts:48-51,93,167-177`）。`?uid=` → ViewTracker → `/api/events`。`src` は未使用。
- ホスティング: 1LC アカウント Amplify `d1j66qnsj1enpq`（スタック `onelc-property`、GitHub narudev4/1lc-property master）。`bukken.1lcinc.com` はこのアプリのカスタムドメイン。Basic 認証は `proxy.ts:52-81`（アプリ側、`SITE_BASIC_AUTH`）。
- WHITELIST 判定 `postmark.mjs:18-26`。不一致は `skipped:not_whitelisted` でキュー書き戻し → **本番稼働中に naru だけにすると当日全員分の配信が失われる。`*` のまま触らない**。
- `emails/property-match.html` は本番テンプレのコピーではない（静的モック）。忠実コピーは `引き継ぎ/toC_mail-matching/postmark_code/v4_lc-v3-prod_修正版.html`。
- Postmark サーバー `CRM`(12912006): TrackOpens=true / TrackLinks=HtmlOnly。直近30日 Sent 4,082 / Clicks 250。追加設定不要。
- `outbound` ストリームに Make.com webhook 4 本（Bounce×2/Click/Open）。テストの開封・クリックが Make に飛ぶ（中身不明）。

## 副次発見（重要）
1. CFn `onelc-delivery` の EventBridge はテンプレ上 `State: DISABLED`（`infra/batch-delivery.yaml:293,304`）、実機は ENABLED（ドリフト）。**`cloudformation deploy` すると翌朝配信が止まる**。反映は S3 + `update-function-code`。
2. ローカル master は origin/master より 3 コミット遅れ、origin/master の `batch/` は fmtYield 修正前。batch 修正は feat で。
3. 公開 API `/api/properties*` が元付会社名・担当・電話・mail を返す（`lib/airtable.ts:24-31,113-116`）。ゲート解除前に除去（D5）。
4. 物件ページは uid 解決で「閲覧中 ○○ 様」表示。テストでは実投資家 uid を使わない。

## 推奨テスト経路 (b')
- 変更 1 `batch/lib/postmark.mjs`: `propertyLink(p, investorId)`（`PROPERTY_LINK_BASE` 未設定=現状維持、pid は `p.pid || bukken_ 正規表現`、取れなければ現状維持、`?uid=..&src=email`）。`sendMatchEmail` に `investorId, tag`。
- 変更 2 `batch/handler-send.mjs`: 本番ループ・testTo で `investorId` を渡す。
- 新規 `batch/run-test-send.mjs`（zip 対象外）: `aws dynamodb get-item --profile 1lc` でキュー 1 件読み取り／`DELIVERY_WHITELIST` が `*` なら中断／`lib/postmark.mjs` のみ import／送信後 `messages/outbound/{id}/details` で本番送信済み HtmlBody と diff（href 以外差分なし）／3 pid の `/api/properties/{pid}` `id === airtable_record_id`。
- 実行例: `AWS_PROFILE=1lc POSTMARK_TOKEN=... DELIVERY_WHITELIST=naru.hosoya@walker-s.co.jp PROPERTY_LINK_BASE=https://bukken.1lcinc.com node batch/run-test-send.mjs --date 2026-08-18 --investor rec0ZHg28FOrYpogM --to naru.hosoya@walker-s.co.jp --uid test-naru-20260818`。

## 本番反映時（後回し）
P1 未公開ゲートの扱い決定（除去／`/property/*` 等の部分開放／当面テストのみ）。P2 公開 API から元付連絡先除去。P3 zip → S3 → `update-function-code`（現行 zip は scratchpad `lambda-send/send.zip` にロールバック用取得済み）。P4 `PROPERTY_LINK_BASE` を env 追加（全変数置換に注意）。P5 CFn に `PropertyLinkBase`・EventBridge `State` パラメータ化。P6 翌朝 8:30 testTo 確認 → 9:00 監視。P7 リポ整備（本番 HTML を `emails/lc-v3-prod.template.html`）。P8 フェーズ 2（物件名・画像リンク化、`src` 保存、`/p/{token}` 化、C7 集計）。

## 未確認・リスク
- Make.com webhook の中身。正規表現抽出の全件網羅は未実施（フォールバックあり）。Postmark リンク書き換え → リダイレクト実クリック未確認。
- 秘密情報の所在: `credentials/postmark-1lc.txt`、`credentials/airtable-1lc-pat.txt`、Secrets Manager `1lc/postmark-token` `1lc/airtable-pat`、プレビュー Basic 認証は Amplify env とマスターシート。
