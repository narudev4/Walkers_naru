# 10. 要件 × 実装 突合レビュー（WS3・walkers-code-review 39 観点・mode=release）

作成: 2026-08-19（ws3-review）。対象: `app` リポ main `3d689ff`（作業ツリー差分なし）。仕様の正: 真理源スプシのスナップショット（8/19 13:26 JST）14_ 画面一覧・07_ 機能一覧・08_ 非機能要件・09_ 技術検証・13_ 異常系・05_ LINE 通知一覧・15_ 継続フロー、`docs/2026-08-17_T-3-test-spec.md`、`docs/2026-08-18_mtg-shiryo-poc-genzaichi.md`。プロファイル: web／api／auth／pii／database／cloud／mobile(LIFF)／ai。
禁止事項の遵守: コードは変更していない。本番へは GET と認可否定系（存在しない UUID・不正キー）だけを送り、Supabase・LINE への書き込み・cron の手動実行はしていない。ローカルの秘密（`.env.local` の service key）を使った Supabase 実測は権限分類器に拒否されたため未実施（該当箇所は「要確認」と明記）。
用語: 「LIFF」＝LINE の中でアプリを開く仕組み、「エンドポイント URL」＝LIFF が最初に開く URL、「cron」＝毎日決まった時刻に動く定期処理、「冪等（べきとう）」＝同じ処理を繰り返しても結果が 1 回分になる性質。

---

## 結論

- 判定: **blocked（現時点）**。理由は 1 点に集約される。LINE 側の入口（通知の着地リンク・リッチメニュー）が LIFF エンドポイント URL＝`/profile` のため 404 になり（hq 確定・High）、T-3 の LINE 実機 3 項目（1-1・1-2・4-6）が未実施のまま「開始できる状態」と報告されている。**エンドポイント URL を `/` に直し（naru の LINE Developers 作業・数分）、1-2／4-6 を実機で PASS させれば conditional に変わる**。それ以外の実装は要件どおり動いており、コード側の Medium 4 件は開始前に短時間で潰せる。
- Critical 0 ／ High 1（S14: LIFF エンドポイント 404・付随して LINE 通数上限）／ Medium 4（D4: N-03 が本番で送られない＋UI の時間帯の約束、F1: 入口が /profile の間はホーム振り分けが効かない＋/hearing のカウンセリングガード未実装、Q3: cron の events 取得が Supabase の Max Rows（既定 1,000 行）で頭打ち、Q7: 要件定義書と定例資料が実装より古い／新しい方向に食い違う）／ Low 8（S3・S4・S9・S10・T1・Q2・Q5・Q8）。
- 「実装済み」表記の過信（依頼 (a)）: F-52「実送信確認済み」は送信ログのみで着地未確認、F-50 ✅ は必須化のみで 13_ 行15（/hearing ガード）は未解消、14_ 行18・07_ F-36/F-42「N-01〜N-12 実装済み」のうち N-03 は日中 cron が無く一度も送られない。逆に 08_ 非機能・09_ 技術検証・14_ の一部は実装より古い（⚠ のまま）。
- 13_ 異常系（依頼 (b)）: 41 行のうち「実装判断」の大半は F-48〜F-51・X-1〜X-3 で解消を file:line で確認。未解消は 行15（/hearing のカウンセリング未回答ガード）・行16（`?flow=step` 試作が本番で有効）・行39（片足立ち秒数＝監修待ち）・行30（先方確認）。
- 実行した検証: `pnpm exec vitest run` → 23 files／173 tests PASS（2 回とも）。`pnpm exec tsc --noEmit` → エラー 0。`pnpm exec eslint .` → 16 errors（react-hooks 系）＋3 warnings（Vercel ビルドは止めない）。`pnpm audit --prod` → 51 件（大半は CLI ツール shadcn の推移依存）。本番: 主要ルート 200、`/profile/today`→307 `/today`（安全網稼働）、`/api/cron/notify` 401、`/api/admin/summary` 401、`/api/review`・`/api/demo` 404、セキュリティヘッダ＋HSTS 確認。
- 残存リスク・未確認: Supabase の Max Rows 設定と events 件数（Q3）、Vercel プラン（cron 精度・関数上限）、LINE プラン（無料 200 通/月）、ADMIN_PASSWORD の強度、LIFF 実機着地（エンドポイント修正後）。

---

## Findings（重大度順）

### [High] S14 LIFF エンドポイント URL が `/profile` のため通知リンク・リッチメニューが 404（hq 確定）／LINE 通数上限で安全通知も止まり得る

- 場所: LINE Developers の LIFF 設定（外部）、`lib/notifications.ts:295-322`（着地リンク生成）、`next.config.ts:29-38`（307 の安全網）、`app/profile/page.tsx:62-71`（liff.state 先行処理）、`vercel.json:3`、`lib/line.ts:47-51`。
- 仕様の出典: 07_ F-52「ディープリンクで通知・リッチメニューから /today 等へ直行 ✅ 実装済み・実送信確認済み」、14_ 行23、05_ 各通知の「▼ …を見る」、`docs/liff-setup.md:16`（必ずルート `/`）。
- 成立経路: `liff.line.me/<ID>/today` → エンドポイント `/profile?liff.state=/today` → LIFF SDK が「エンドポイントのパス＋state」を連結して `/profile/today` へ → 404。安全網（307）は本番で稼働を確認したが、根本対処（エンドポイントを `/` に変更）は naru の手作業待ち。この間、リッチメニュー「アプリをひらく」（パス無し）は `/profile` に着地し、登録済み参加者にも基本情報フォームが出る（F1）。
- 付随: LINE Messaging API の月 200 通（無料）を、利用者宛の毎晩のリマインドとトレーナー宛の安全通知（N-09/N-10）が同じ枠で消費する。上限到達後は `pushLine` が失敗し、痛み申告の即時通知も届かない（画面は「送信できませんでした」に切り替わるが、トレーナー側には何も来ない）。プラン切替は定例で相談中。
- 影響: PoC の入口と「続ける」導線が最初の通知から機能しない。
- 最小修正: エンドポイント URL を `https://ptj-poc-ai-trainer.vercel.app/` に変更 → 実機で 1-2／4-6 を確認して T-3 に記録。LINE はライトプランへ（要 naru 判断・先方負担合意）。
- 必要テスト: T-3 1-1・1-2・4-6（📱）。

### [Medium] D4 N-03（実施予定日の「選んだ時間帯」の通知）が本番で一度も送られない一方、画面は時間帯の通知を約束している／N-06 の文面が事実と違う／/hearing のカウンセリングガード未実装

- 場所: `vercel.json:3`（cron は 11:00 UTC＝20:00 JST 1 本のみ）、`lib/notify-triggers.ts:288-292`（N-03 は slot=day のときだけ）、`components/result/consent-section.tsx:382`（「{曜日}曜日の{時間帯}に、LINEでお知らせします」）、`lib/notifications.ts:184-189`（N-06「先週いただいたご回答をもとに、内容を少し調整しています」）、`app/hearing/page.tsx:55-58`（ガードは ptj.profile のみ）。
- 仕様の出典: 05_ N-03「ご自身で選んだ実施予定日の、選んだ時間帯」、14_ 行18・07_ F-36/F-42「N-01〜N-12 実装済み」、07_ F-45（/plan は「同じメニューで継続」に簡素化）、13_ 行15（/hearing ガードは F-50 で対応）。
- 成立経路: cron は夜 20 時のみ → `slot` は常に `evening` → N-03 は候補にならず N-04 のみ。利用者が「朝」を選んでも朝には何も届かない。N-06 は毎週「調整しています」と送るが /plan は同じメニュー。/hearing はカウンセリング未完了でも直接入れる（危険信号判定はカウンセリング回答が無いと働かない）。
- 最小修正: (1) 時間帯に合わせた日中 cron（例 08:00／12:00／18:00 JST）を追加して `?slot=day` で呼ぶか、UI 文言を「予定日の夜にお知らせします」に直す（要 naru 判断）。(2) N-06 文面を「来週も同じメニューで続けましょう」系に。(3) /hearing のガードに `ptj.counseling` を追加（`mode=recheck` は除外）。
- 必要テスト: notify-triggers に slot=day のケースは既にある。cron 追加時は Vercel の cron 実行ログで確認。/hearing のガードは hearing.test.tsx に 1 件追加。

### [Medium] F1 入口が `/profile` の間は「プラン済みなら /home」の振り分けが効かず、登録済み参加者が毎回フォームに着地する／`?flow=step` 試作が本番で有効

- 場所: `app/page.tsx:30-32`（振り分けは / のみ）、`app/profile/page.tsx:36-60`（保存値をプリフィルするだけ）、`app/hearing/page.tsx:78`（flow=step）。
- 仕様の出典: 07_ F-01「プラン保存済みなら /home へ自動遷移」、`docs/2026-08-06_richmenu-draft.md:10`（「アプリをひらく」はトップの振り分けに依存）、13_ 行16（試作は封鎖 or ガード）。
- 影響: S14 が直るまでの間、リッチメニューから入る 2 日目以降の参加者の体験が壊れる。エンドポイント修正で解消するが、設定の戻りに備え `/profile` にも保険を置くのが安全。
- 最小修正: `profile/page.tsx` の `proceed()` 先頭で `readRaw('local','ptj.plan')` があれば `router.replace('/home')`。`?flow=step` は本番ビルドで無効化。
- 付随（Low）: フィードバック送信は fetch 失敗でも成功表示（`components/result/feedback-form.tsx:56-69`。07_ F-17「残」と一致）。

### [Medium] Q3 日次 cron の events 取得が Supabase の Max Rows（既定 1,000 行）で頭打ちになり、超えた夜から誤配信が静かに始まる

- 場所: `app/api/cron/notify/route.ts:51-55`（events 全件を 1 回の select・`.limit(20000)`・ORDER なし）、`lib/notify-triggers.ts:372-381`。
- 仕様の出典: 07_ F-42「重複ガード・1日1通」、05_ 既定「1日1通まで」。
- 成立経路: Supabase（PostgREST）は「Max Rows」設定（既定 1,000）で 1 リクエストの行数を上限し、クライアントの limit では超えられない（公式ドキュメント／GitHub Discussion #1742・#3765）。events が 1,000 行を超えると cron は古い順に 1,000 行しか見えず、当日の training／skip・直近の notify ログ・曜日変更が判定に入らない → 実施済みの人に N-04、生涯 1 回の N-02/N-07/N-11 の再送、1 日 1 通ガードの欠落。規模感: 参加者 1 人あたり月 60 件前後＋テスト・デモ由来のイベントも同じ表にあるため、10〜15 名で 1 か月以内に到達し得る。
- 未確認: 現在の events 件数と Max Rows 設定（権限分類器で実測不可）。設定が引き上げ済み・参加者が少数なら発火しない。
- 最小修正: (a) `user_id in (非デモ users)` で絞る、(b) `type in (判定に使う 8 種)` に限定、(c) `order created_at desc` ＋ range で 1,000 件ずつ全件読む。加えて naru が Supabase → Settings → API → Max Rows と events 件数を確認（1 分）。
- 必要テスト: cron の取得部を関数化し、Supabase をスタブして「1,000 件超でも全件」を固定（T1）。

### [Medium] Q7 要件定義書と定例資料が実装とずれている（古い方向と新しい方向の両方）

- 場所: `docs/2026-08-18_mtg-shiryo-poc-genzaichi.md:16-23`、`docs/2026-08-17_T-3-test-spec.md:11-12,48,27,36-37,75`、スプシ 14_/07_/08_/09_/05_/13_（下記「スプシへの反映提案」）。
- 内容: 定例資料 §1「全画面・全機能の実装が完了し、テスト（自動確認26項目）を通しました」「残っているのは動画の反映と参加者への案内だけ」に対し、T-3 は 📱 3 件・⏳ 4 件が未実施で、4-6 は 8/19 に 404 と判明。07_ F-52「実送信確認済み」は送信ログのみ。一方で 08_ 非機能要件（7/29 基準）と 09_ 技術検証 行8〜10 は「⚠ 未実装／未検証」のままで、S-1〜S-10 で実装・実測済みの事実を反映していない。要件定義書としての信頼性（本開発見積の根拠）に関わる。
- 最小修正: 「スプシへの反映提案」の行単位修正を naru 承認後に反映。定例資料 §1 は「LINE 入口の最終確認 3 点が残る」に改める。

### [Low] S3 管理・レビュー・デモの共有パスワードに試行回数制限が無い

- 場所: `app/api/admin/summary/route.ts:13`、`lib/review-server.ts:8-31`。
- 内容: 定数時間比較・env 未設定は拒否で堅いが、ロックアウト・遅延・レート制限が無い。パスワードが短ければ総当たりで参加者全員の健康情報と一言送信に到達できる。実強度は未確認。
- 最小修正: ADMIN_PASSWORD を 20 文字以上のランダム値にする運用を明記（要 naru 確認）。余裕があれば失敗 N 回で 429。

### [Low] S4 UUID を知れば読める／書ける無認証 API（PoC の設計判断として受入条件に明記を推奨）

- 場所: `app/api/restore/route.ts:34`、`app/api/baseline/route.ts:34`、`app/api/messages/route.ts:15`、`app/api/users/route.ts:21`、`app/api/alert/route.ts:87`。
- 内容: 利用者の UUID（`ptj.userId`）が事実上の合言葉。v4 UUID で推測不能・存在しない UUID には空／404 を返すことを本番で確認。UUID は URL クエリで飛ぶため Vercel のログ・端末履歴に残る。T-3 8-11 は /api/alert のみ言及。
- 推奨: 要件定義書（17_ 線引き）に「PoC は UUID ベアラー方式を許容、本番は LIFF ID トークン検証」を明記（要 naru 判断）。

### [Low] S9 `/api/users` に上限が無く、偽の利用者行で /admin 一覧を汚染できる

- 場所: `app/api/users/route.ts:21`、`app/api/cron/notify/route.ts:45`。
- 最小修正: IP 単位の簡易レート制限、または /admin 一覧を「LINE 連携あり or イベントあり」に絞る。

### [Low] S10 依存の監査結果と配置

- 場所: `package.json:20`（shadcn が dependencies）、`package.json:9`（@line/liff）。
- 内容: `pnpm audit --prod` 51 件のうち shadcn（CLI・未 import）由来が 32 件、@line/liff（axios）11 件、next 推移 8 件。実行時到達性は限定的。
- 最小修正: shadcn を devDependencies へ、`pnpm update @line/liff next`。

### [Low] T1 サーバールートの自動テストがゼロ

- 場所: `app/api/users/route.ts:49`（X-1 衝突経路）、`app/api/cron/notify/route.ts:31`、`app/api/alert/route.ts:87`。
- 内容: 173 件はロジック・部品・同意ガードを厚くカバーするが、API ルート（衝突・レート制限・認証・cron の取得と送信ループ）は手動実測のみ。Q3 のような「クエリの前提」もテストで固定されていない。

### [Low] Q2 storage.ts を経由しない localStorage 直叩き 19 箇所と重複ヘルパ、デッドコード

- 場所: `app/page.tsx:30`（入口・try 無し）、`app/today/page.tsx:134`・`app/plan/page.tsx:27`（formatLocalDate 重複）、`app/api/alert/route.ts:39`（honorific 重複）、`lib/demo-diagnosis.ts`（参照ゼロ）。
- 内容: 07_ F-48「storage アクセスの try 内移動 ✅」と部分的に食い違う（入口の 1 行は storage 不能環境でエラー境界に落ちる。実発生は稀）。
- 最小修正: `page.tsx:30` を `readRaw('local','ptj.plan')` に。

### [Low] Q5 失敗（push-failed・cron 例外）を人に知らせる仕組みが無い

- 場所: `app/api/cron/notify/route.ts:114-123`、`lib/line.ts:47`。
- 内容: T-3 8-10 の「週次で cron ログ確認」に依存。安全通知の失敗はトレーナーに届かない（S14 の通数上限と重なる）。
- 最小修正: cron 応答の skipped に push-failed があれば naru の LINE へ 1 通知（資料 §6 の「当面は細谷が受ける」体制と整合）。

### [Low] Q8 同意文と実データの流れの差

- 場所: `app/consent/page.tsx:55-100`、`app/api/feedback/route.ts:24-38`。
- 内容: 同意文は預かる情報 5 種・目的・第三者提供なし・終了時に扱いを決定と明記（08_ の ⚠ は解消）。ただしフィードバック（氏名・年齢・性別・身長・体重）が GAS 経由で Google スプレッドシートへ送られる点と、委託先（Google／LINE／Supabase）の記載が無い。保持期間・削除手順は W-08 待ち。
- 要 naru 判断: 同意文に一文足すか、古谷さんの規約レビューに委ねるか。

---

## 確認したが所見にしなかった主な項目（pass の根拠）

- 同意ガード（/profile → /consent、消えた同意の復元）と安全系必須化: `consent-safety.test.tsx` 16 件・実装 `profile:42-45`、`counseling:777`。
- 危険信号 3 条件（痛み 7 以上・運動制限・痺れ 7 以上）＋発熱・めまい・安静時痛 → 注意バナー＋N-10: `result-client.tsx:47-99`。痛み申告の実送信と成否表示: `today.tsx:212-224,617-621`。
- イベント基盤: クライアント UUID を PK にした冪等 INSERT・409＝送信済み・失敗はキュー再送・4xx は破棄（`events.ts`、テスト）。RLS: anon は events INSERT のみ、users／trainer_messages／user_summary は遮断（`schema.sql:81-121`、T-3 6-6 実測、本番でも 401/404 再確認）。
- X-1（再登録の LINE 連携引き継ぎ）: `api/users:49-66` → クライアント `events.ts:126-150` → 復元再実行。X-2 下書き保存: hearing／counseling の draft キー。
- 通知判定: 起算日＝最初の diagnosis・固定週・生涯 1 回・同日 1 回・1 日 1 通・N-12 例外・demo 除外・終了後停止（`notify-triggers.ts`、テスト 21 件）。cron は Bearer CRON_SECRET・fail closed（本番 401）。
- 秘密: 追跡ファイル・履歴に実値なし、サーバー専用モジュールを client から import していない、定数時間比較。
- ヘッダ: X-Frame-Options DENY・nosniff・Referrer-Policy・HSTS を本番で確認。liff.state は SDK 実装上、同一オリジンのパスにしか飛ばない（open redirect ではない）。
- 境界値: プロフィールは zod（年齢 20〜100 等）、自由入力は 200〜300 字、日付は JST 固定でテスト済み。
- 復元・失敗経路: restore 8 秒タイムアウト、診断 15 秒＋再試行、LINE 10 秒、push 失敗は翌日再判定。

---

## Scope and verification

- モード: release。プロファイル: web／api／auth／pii／database／cloud／mobile／ai。基準点: main `3d689ff`。
- 読んだ指示・仕様: 00-BRIEF、AGENTS.md／CLAUDE.md、スナップショット 14_/07_/08_/09_/13_/05_/15_（他タブは索引のみ）、docs（T-3 仕様書・定例資料・liff-setup・richmenu-draft・line-id-recovery）。
- 読んだコード: `app/**`（16 画面・API 13 ルート）、`lib/**`（events／restore／storage／liff／line／notifications／notify-triggers／poc-config／supabase-server／review-server）、`db/schema.sql`、`next.config.ts`、`vercel.json`、`package.json`、`tests/**`（件数と対象の突合）、`@liff/init` の decodeState（node_modules）。
- 実行コマンドと結果: `pnpm exec vitest run` → 173/173 PASS（2 回）。`pnpm exec tsc --noEmit` → 0。`pnpm exec eslint .` → 16 errors／3 warnings（react-hooks/set-state-in-effect 15・react-hooks/refs 1・no-unused-vars 2・exhaustive-deps 1）。`pnpm audit --prod` → 51 件（内訳は S10）。
- 本番（読み取りのみ）: 16 ルートの HTTP コード、307 安全網、401/404 の認可否定系、存在しない UUID での /api/restore・/api/baseline・/api/messages（空応答）と /api/alert（404）、不正 body の /api/users（400）、レスポンスヘッダ。
- 実行できなかった検証: Supabase の events 件数・Max Rows 設定（秘密の使用を拒否）、anon キーでの遮断再現（同上。T-3 6-6 の記録に依存）、LIFF 実機（LINE 端末が必要）、Vercel／LINE のプラン確認。

---

## Coverage

| 状態 | 件数 |
|---|---:|
| finding | 13 |
| pass | 25 |
| na | 1 |
| blocked | 0 |

- 重大度: Critical 0 ／ High 1（S14）／ Medium 4（D4・F1・Q3・Q7）／ Low 8（S3・S4・S9・S10・T1・Q2・Q5・Q8）。
- na: S8（ファイル・パーサーの受け口なし）。
- blocked: なし（確認できなかった項目は各所見に「要確認」として明記）。
- 観点別の証拠全文は状態ファイルからの export（`coverage.md`・スクラッチ領域）にあり、必要なら本文書へ追記する。

---

## Release gate（PoC 開始判定）

- 現時点: **blocked**。LINE 入口（通知着地・リッチメニュー）が 404 で、T-3 の LINE 実機 3 項目が未実施。
- **conditional に変える条件（開始日までに）**: ① LIFF エンドポイント URL を `/` に変更し、T-3 1-2・4-6 を実機 PASS（naru）。② `/profile` にプラン済み→/home の保険 1 行、`/hearing` のカウンセリングガード、`page.tsx:30` の安全化（合計 30 分以内）。③ Q3 の cron 取得を絞り込み＋ページング（1〜2 時間）か、少なくとも Max Rows と件数を確認して閾値を運用文書に書く。④ LINE プランの切替（先方合意）または上限到達時の運用（naru が cron の push-failed を毎日確認）を決める。⑤ N-03／時間帯の約束（D4）と N-06 文面を naru が判断（cron 追加か文言修正か）。
- 開始後 1 週間以内: T-3 ⏳ 4 件（2-4・3-2・3-3・7-1）の消化、Q7 のスプシ反映、Low 8 件。
- 上記は記載したスコープ（app リポ HEAD 3d689ff・8/19 13:26 のスプシ）に限った判定で、動画反映（V 系）と先方合意事項（W-01〜）は含まない。

---

## スプシへの反映提案（hq が naru 承認後に反映）

| タブ | 行・列 | 現状の記載 | 提案 |
|---|---|---|---|
| 14_ 画面一覧 | 行5（/profile）K 列 | 「ptj.profile・ptj.demo（sessionStorage）」 | 「ptj.profile（localStorage・U-2）＋events(register)。demo は sessionStorage」。L 列の「保存が try 外」は解消済みに更新。 |
| 14_ 画面一覧 | 行8（/hearing）K・L 列 | 「sessionStorage。全動作完了時に一括」「動画再生失敗の代替なし／途中リロードで最初から／中断・再開なし」 | 「localStorage＋下書き ptj.hearingDraft（X-2）」。動画 onError・下書き再開は解消済み。**「カウンセリング未回答でも直接入れる」は未解消のまま残す**。 |
| 14_ 画面一覧 | 行9（/result）L 列 | 「profile 破損でクラッシュ／タイムアウトなし」 | 「解消済み（F-48: readJson・15 秒タイムアウト＋再試行）」。 |
| 14_ 画面一覧 | 行11（/plan）K 列 | 「読: なし → 書: なし（←修正対象）」 | 「読: ptj.log → 書: events(plan weekly_continue)」。 |
| 14_ 画面一覧 | 行13（/monthly）K 列 | 「書: なし（←修正対象）」 | 「書: ptj.monthlyReports＋events(monthly)」。 |
| 14_ 画面一覧 | 行17（/admin）L 列 | 「無認証・ダミー混在」 | 「解消済み（F-39）。総当たり対策なし（S3）」。 |
| 14_ 画面一覧 | 行18（LINE通知）E・F 列 | 🟡「残: LINE公式アカウント作成→トークン設定→実機打鍵」 | 「実送信は 8/7〜実績あり。**N-03（日中）は cron 未設定のため未配信**。着地リンクは LIFF エンドポイント修正待ち」。 |
| 14_ 画面一覧 | 行21（/consent）L 列 | 「未同意のまま /profile へ直行できないようガードが必要」 | 「ガード実装済み（profile:42）」。 |
| 14_ 画面一覧 | 行23（LINE公式）E 列 | ⬜ 未着手 | 🔴 要対応「アカウント・リッチメニューは作成済み。LIFF エンドポイント URL が /profile のため 404（8/19）。/ へ変更が必要」。 |
| 07_ 機能一覧 | F-36／F-41 F 列 | 「残: LINE公式アカウント作成→…実機打鍵」 | 「実送信済み（T-3 4-2〜4-5）。N-03 は日中 cron 未設定で未配信。着地は F-52 参照」。 |
| 07_ 機能一覧 | F-50 F 列 | 「安全系4項目必須化＋痺れ必須化」 | 末尾に「/hearing のカウンセリング未回答ガード（13_ 行15）は未対応」を追記。 |
| 07_ 機能一覧 | F-52 E・F 列 | ✅「実送信確認済み」 | 🔴「送信ログ ok=true は確認済み。着地は 8/19 に 404（エンドポイント /profile）。安全網 307 は本番反映済み。エンドポイント修正と実機 4-6 で ✅ へ」。 |
| 07_ 機能一覧 | F-24／F-45 F 列 | 「同じメニューで継続」 | 「N-06 の文面（調整しています）を実態に合わせて修正すること」を追記。 |
| 05_ LINE通知一覧 | N-03 B 列 | 「選んだ時間帯」 | 「※ PoC では夜 20 時の N-04 のみ配信。時間帯どおりの配信は日中 cron 追加が必要（要判断）」を注記。 |
| 05_ LINE通知一覧 | N-05／N-06 B 列 | 「週の最後の実施日の夜」「日曜の夜など」 | 「起算日から 7 日ごとの固定週の最終日の夜」「固定週の初日の夜」（F-42 準拠）。N-06 D 列は F-45 に合わせて修正案を併記。 |
| 05_ LINE通知一覧 | N-11 B 列 | 「友だち追加から24時間」 | 「アプリを開いて同意してから 24 時間（友だち追加自体は検知できない）」。13_ 行34 も同様。 |
| 08_ 非機能要件 | 権限管理 D 列 | 「⚠ /admin に認証なし」 | 「PoC: 共有パスワード認証（x-admin-password）。試行回数制限なし」。 |
| 08_ 非機能要件 | データ保存 D 列 | 「⚠ localStorage のみ…接続が未実施」 | 「Supabase 接続済み（S-1/S-2・RLS 実測 8/6）。localStorage は UI の正、DB は追記ログ」。 |
| 08_ 非機能要件 | 通知（LINE）D 列 | 「⚠ 未実装。文言も未受領」 | 「実装・実送信済み（N-03 除く）。文言は仮のまま（枝光様回答待ち）」。 |
| 08_ 非機能要件 | 個人情報・利用規約 D 列 | 「⚠ 未作成」 | 「/consent 実装・本番反映済み。委託先の記載と終了後の扱い（W-08）が残」。 |
| 09_ 技術検証 | 行8（データ永続化）C 列 | 「⚠ 未検証」 | 「○ Supabase 接続・RLS・通し確認 46/46（8/6）」。 |
| 09_ 技術検証 | 行9（LINE 通知）C 列 | 「⚠ 未検証」 | 「○ 実送信 8/7〜（N-02/04/05/10）。着地は 8/19 404 → 修正中」。 |
| 13_ 異常系 | 行4・6・12・17・19・20〜22・25〜28・31〜33・35・38 | 「あるべき挙動（案）」のみ | 「対応状況」列（新設）に「解消済み（F-48 等・file:line）」を追記。 |
| 13_ 異常系 | 行15・16 | 「実装判断」 | 行15「未解消（/hearing は profile のみ判定）」、行16「未対応（?flow=step 本番で有効）」。 |
| 15_ 継続フロー | ステップ 9・13 E 列 | 「前回の回答を反映したメニュー調整」「翌週プランの自動生成」 | 「PoC は同一メニュー継続（F-45・8/5 決定）。反映は本番スコープ」を注記。 |

---

## 要 naru 判断（列挙）

1. N-03（時間帯どおりの通知）を日中 cron 追加で実現するか、UI 文言を「予定日の夜」に直すか（D4）。
2. LINE プランの切替（ライト月 5,000 円）と費用負担、または上限到達時の運用（S14）。
3. Supabase の Max Rows と events 件数の確認、cron 取得の絞り込み実装を開始前に入れるか（Q3）。
4. UUID ベアラー方式（無認証 API 4 本）を PoC の受入条件として明記するか（S4）。
5. 同意文への委託先（Google／LINE／Supabase）・フィードバック保存先の追記か、規約レビューへ委ねるか（Q8）。
6. ADMIN_PASSWORD の強度確認とレート制限の要否（S3）。
7. 定例資料 §1 の表現修正（「LINE 入口の最終確認 3 点が残る」）。

---

## hq への報告

- 成果物: `03_projects/パーソナルトレーナージャパン/review/2026-08-19_要件定義レビュー/10-要件×実装レビュー_ws3.md`（状態ファイル・coverage.md はスクラッチ領域）。
- 39 観点すべて記録・validate 通過。finding 13（High 1・Medium 4・Low 8）／pass 25／na 1／blocked 0。
- High 1: S14 LIFF エンドポイント 404（hq 確定）＋LINE 通数上限で安全通知も止まり得る。
- Medium 4: D4 N-03 が本番未配信＋UI の時間帯約束・N-06 文面・/hearing ガード／F1 /profile 着地の振り分け不在／Q3 cron の events 取得が Max Rows 1,000 で頭打ち（要確認）／Q7 資料と実装のずれ。
- 判定: 現時点 blocked（LINE 入口）。エンドポイント修正＋実機 1-2/4-6 PASS＋コード側 30 分の保険で conditional。
- 検証: vitest 173/173・tsc 0・eslint 16 errors（react-hooks）・本番 GET/否定系のみ。Supabase 実測は秘密使用が拒否され未実施。
