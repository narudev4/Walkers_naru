# PTJ 要件定義レビュー・テストケース深掘り 統合レポート（2026-08-19 14:05 JST・hq 作成）

> 体制: hq（本セッション）＋他セッション 3（ws3-review＝要件×実装突合／ws2-testcases＝詳細テストケース／ws1-manual＝質問棚卸し＋LINE/LIFF 設定手順）＋多モデルパネル 6（haiku×2・sonnet×2・opus×2）。成果物はすべて本ディレクトリ。スプシは読み取りのみ（反映提案は §7、naru 承認後に hq が反映）。

## 1. 結論（3 行）

- **LINE 通知 URL・リッチメニュー→404 の原因は LIFF エンドポイント URL が `/profile` になっていたこと**（コンソール外から `curl https://liff.line.me/2010137019-OCaRjDeQ/today` で実測・SDK の `decodeState` で二次遷移先が `/profile/today` になると確定）。コード側の安全網を本番反映し、**いま通知リンク／リッチメニューは開ける**（Chrome で SDK 二次遷移→307→`/today` 表示まで E2E 確認）。根本対処（エンドポイントを `/` に変更）は naru の「更新」ボタン 1 回待ち。
- 要件定義書（画面一覧・機能一覧・非機能・技術検証・工程表・質問）は**「先方に見せる 4 枚のうち非機能・技術検証・0_INDEX が 7/29 のまま」＝実装より悪く見える**のが最大の文書問題。加えて「N-03 が本番で一度も送られない」「N-08 が 1 日 1 通枠を食って N-05/N-07 が永久に落ちる」「cron が Max Rows 1,000 で頭打ち」の 3 つの実装穴をコードで確定（後 2 つは修正・デプロイ済み）。
- PoC 開始判定: **conditional**。案 A（8/24）推奨。今日の定例で「LINE 料金プラン」「W-01/02/05/08/12」「稼働超過の打診」が片づけば開始可（詳細 §4）。

## 2. 本日 hq が修正・本番反映したもの（`app/` main・Vercel 自動デプロイ・全 vitest 175/175）

| コミット | 内容 | 検証 |
|---|---|---|
| `3d689ff` | LIFF ディープリンク 404 の安全網: `next.config.ts` に `/profile/today`→`/today` 等の 307、`/profile` で `liff.state` 先行 init、docs/liff-setup.md 修正 | 本番 curl 307 確認・Chrome で `/profile?liff.state=%2Ftoday`→`/today` 表示 |
| `eeaf2d8` | 同意画面 U3-06（初期表示で「同意してはじめる」が画面外）: 規約枠 40vh＋ボタン領域 sticky | 本番 Chrome で初期表示にボタンあり |
| `9ffcd68` | N-08（トレーナー一言）が「1 日 1 通」枠を消費し、同日の N-05／N-07 が永久喪失 → cron 配信だけを枠に数える | 回帰テスト 2 件追加（notify-triggers 23/23） |
| `204e058` | cron の events 取得を 1,000 行ずつ range ページング（PostgREST Max Rows 対策。10 名×30 日で 1,000 行を超える） | tsc/eslint/vitest 通過 |
| `051bf11` | `robots: noindex`（LINE 友だち限定アプリの検索露出防止） | tsc 通過 |

## 3. naru の手作業（残 1 件・1 分）

- LINE Developers（開いたタブ）で LIFF `2010137019-OCaRjDeQ` の**エンドポイント URL を `https://ptj-poc-ai-trainer.vercel.app/` に書き換えて「更新」**（フォーム送信は私の権限で止められる）。反映は hq がバックグラウンドで自動検知して報告する。
- 効果: 404 の根本解消に加え、リッチメニュー「アプリをひらく」（bare LIFF URL）が `/`→プラン有無で `/home` 振り分けに戻る（現状は登録済みの人が毎回 `/profile` の入力フォームに着地する。ws3 F1）。

## 4. 15:00 定例で扱うべきこと（優先順）

1. **PoC 開始日**: 案 A 8/24（推奨）／案 B 8/21（枝光様の声かけが 1 日しかない）。opus 工程表パネル判定は「案 A 条件付き可」。
2. **LINE 料金プラン**: 無料 200 通/月は 10 名で月中に止まる。安全通知（N-09/N-10）も同じ枠なので、上限到達＝痛み申告が届かなくなる（ws3 High）。ライトプラン切替の費用負担を今日決める。
3. **稼働超過の打診**（定例資料に 1 行も無い）: 稼働計画タブは 7/29 のまま。8/5 以降 43 行が未反映、残作業 13〜16h に対し留保 4.5h（opus 45 番）。マニュアル「黙って超過させない」に照らし今日が打診期限。
4. **先方回答 6 件を口頭で確定**（W-01 人数・W-02 終了日・W-05 24h 連絡・W-08 データ扱い・W-11 管理画面範囲・W-12 危険信号）。実装と規約の両方をブロック中（opus 44 番）。ws1 の「決めて報告・拒否権だけ渡す」型の完成文は `30-質問棚卸し_ws1.md` §3。
5. 画面共有の注意: **PoCスケジュール／稼働計画／プロジェクト概要の 3 タブは開始日 8/18 のまま**（8/21・8/24 はスプシ全タブに 0 件）。

## 5. 確定所見（重複を統合・証拠付き。詳細は各ファイル）

### 実装（High）
| # | 所見 | 証拠 | 状態 |
|---|---|---|---|
| I-1 | LIFF エンドポイント `/profile` による全ディープリンク 404 | liff.line.me 応答 HTML・`@liff/init` decodeState | 安全網デプロイ済み・根本対処 naru 待ち |
| I-2 | LINE 無料枠 200 通/月に安全通知も含まれ、上限で痛み申告が止まる | `lib/line.ts` pushLine 単一経路・定例資料 §5 | 要決裁（定例） |
| I-3 | N-03「今日はトレーニングの日」（選んだ時間帯）は `slot==='day'` 限定・本番 cron は 20:00 evening 1 本のみ → **一度も送られない**。一方 `consent-section.tsx:382` は「{曜日}の{時間帯}にお知らせ」と約束 | `lib/notify-triggers.ts:288`・`vercel.json` | **要 naru 判断**（朝 cron 追加 or N-03 を対象外にして通知一覧・画面文言を直す） |

### 実装（Medium・修正済み or 要判断）
| # | 所見 | 状態 |
|---|---|---|
| I-4 | N-08 が 1 日 1 通枠を消費→同日の N-05/N-07 永久喪失 | 修正済み（`9ffcd68`） |
| I-5 | cron events 全件取得が Max Rows で頭打ち→誤配信 | 修正済み（`204e058`） |
| I-6 | 同意画面ボタンが初期表示で画面外（U3-06） | 修正済み（`eeaf2d8`） |
| I-7 | 危険信号（/result）・相談（/weekly）の `/api/alert` 失敗が無音。痛み申告（/today）だけ成否表示 | 要判断（30 分。文言設計要） |
| I-8 | `TRAINER_LINE_USER_ID` 未設定時は dry-run で `ok:true` → 利用者に「共有しました」と出るが誰にも届かない | 運用手順書に「開始前に必ず設定」明記＋開始前チェック（ws1 31 番）で担保 |
| I-9 | `POC_END_DATE` 未設定（実測 `vercel env ls`）→ 終了案内 N-12・停止が永久に発火しない | W-02 確定後に設定（1 分） |
| I-10 | AI 診断文が病名（「脊柱管狭窄症の可能性」等）を返す。規約「病気の診断ではない」と矛盾 | 要判断（枝光様監修前に言い換え先行するか） |
| I-11 | 規約に要配慮個人情報の明示・保持期間・委託先・事業者情報・問い合わせ窓口が無い。古谷さんレビュー未 | 要判断・W-08 依存 |
| I-12 | /hearing にカウンセリング未回答ガード無し（画面一覧 13_ 行 15 の穴は未解消。F-50 ✅ は必須化のみ） | 20 分で実装可 |
| I-13 | `?flow=step` 試作経路が本番で有効 | 削除 or キー制（10 分） |
| I-14 | 通知失敗（push-failed）・cron 例外を人に知らせる仕組みが無い | 要判断（Slack/メール 30 分） |

### 文書（要件定義書としての問題）
| # | 所見 | 出所 |
|---|---|---|
| D-1 | 非機能要件シート 18 行中 11 行・技術検証結果・0_INDEX が 7/29 のまま。「/admin 認証なし」「データ永続化未検証」「LINE 通知未検証」が残り、**できているのに未対応と読める**（本見積もりの根拠として不適） | 43 B-1/2・44 §6・45・46 |
| D-2 | 機能一覧 J 列が「本番/PoC 限り」でなく「PoC 完了要否」の分類になっている。本番必要だが行が無い機能 12 件（月次 AI 総評 F-58 は X-9 期限 9/17 で決定済みなのに行が無い） | 46 |
| D-3 | 画面一覧の「主な機能」列に F-51（危険信号）・F-52（ディープリンク）が紐づいていない。`/plan`・`/monthly` の「データ」列が「書き込みなし・修正対象」のまま（S-7/S-8 で実装済み） | 43 A-3/4・B-3/4 |
| D-4 | 作業一覧・機能一覧・画面一覧・確認事項の ID セル（例「8-7」「6-4」）が**日付型**で保存されている（XLSX で実測: 作業一覧 39・機能一覧 28・画面一覧 12・確認事項 6 セル）。表示は正しく見えるが並べ替え・VLOOKUP・API の UNFORMATTED 読みで壊れる | 43 A-1（hq 再検証） |
| D-5 | 工程表に「LIFF エンドポイント修正＋着地確認」行が無い（T-6 は ✅ のまま）。期限超過 10 件（Walkers 起因 4）。NA-003/004/005 は作業一覧に対応行なし | 45 |
| D-6 | 「実装済み ✅」の信頼性: F-52「実送信確認済み」だが着地は未確認。T-3 の 📱3・⏳4・⚠1 が未実施のまま定例資料 §1 は「開始できる状態」 | 41・45・ws3 Q7 |
| D-7 | 質問管理: 確認事項／ご質問・ご相談／NA／作業一覧「相手待ち」に同じ論点が別 ID で 2〜4 か所重複（12 論点）。確認事項に起票日・期限列が無い | 30 |
| D-8 | 先方向け資料に内部用語（LIFF・localStorage・cron・RLS…15 語）と 🔴 が説明なく露出。PoC の**成功基準（継続率何%で本開発へ）が資料のどこにも無い** | 41 |
| D-9 | 利用者目線: Day1 朝に通知が来るかが分からない（→ I-3 と同根）、痛み申告後に何が起きるかが Day1 に見えない、通知 12 通中 7 通が △× | 42 |

## 6. 要 naru 判断（一覧）

1. **N-03 の扱い**（朝 cron を足す／N-03 を PoC 対象外にして通知一覧・同意画面文言を直す）。
2. **LINE 料金プラン**の費用負担（定例）。
3. `TRAINER_LINE_USER_ID` を枝光様に切り替える基準（現状 naru 宛＝定例資料 §6 どおり。安全通知だけ枝光様、は追加実装要）。
4. 病名表示の言い換えを枝光様監修前に先行するか。
5. 危険信号・相談の送信失敗を画面に出すか（文言）。
6. 日次監視 15 分×30 日（≒7.5h）を残稼働でどう賄うか（自動通知＋週 2 回監視が現実案）。
7. スプシ反映（§7）を hq が一括で行ってよいか。

## 7. スプシへの反映提案（承認後に hq が実施・書き込み前に再読）

各ワーカーの「スプシへの反映提案」節を統合した要点。行番号は各ファイル参照。
- 非機能要件シート: 11 行を実装実態に差し替え（44 §6 の表）。技術検証結果: 未検証 3 件→検証済みに更新（Supabase 永続化・LINE 実配信 8/7〜・LLM は方針決定済み）。0_INDEX 行 23〜24 の ⚠ 文を更新。
- 機能一覧: F-52 の「実送信確認済み」→「着地は 8/19 安全網で解消・エンドポイント修正待ち」、F-33〜35 実装方法列 localStorage→Supabase、F-41/F-43/F-47 の dry-run・POC_END_DATE 記述を実測に合わせる、J 列を「本番/PoC/要判断」に定義し直し＋本番候補 12 行追加。
- 画面一覧: `/plan`・`/monthly` データ列、F-51/F-52 の紐づけ、`/`（トップ）の「振り分けはエンドポイント `/` 前提」注記、`/consent` U3-06 解消。
- 作業一覧: 「LIFF エンドポイント修正＋着地確認」行を新設、6-5 を完了に、期限超過 10 件の期限更新、NA-003/004/005 の対応行追加、X-8 運用手順書・T-5 管理画面手順の期限。
- 稼働計画: 8/5 以降 43 行の稼働を追記し残稼働を再計算（打診の根拠）。PoCスケジュール／プロジェクト概要: 開始日 8/18→決定日。
- LINE通知一覧: N-03 の扱い（判断 1 に従う）、N-06 文面を F-45 簡素化に合わせる、N-08 が 1 日 1 通枠外である旨。
- 確認事項／ご質問: 重複 12 論点の統合、起票日・期限列の追加、W-01〜13 の書き換え（30 §3）。
- ID セル（D-4）: 該当列を「書式なしテキスト」にしてから値を再入力（hq がスクリプトで実施可）。

## 8. 成果物一覧

| ファイル | 担当 | 要点 |
|---|---|---|
| `10-要件×実装レビュー_ws3.md` | ws3-review（walkers-code-review release） | 39 観点 validate 通過。finding 13（High 1・Medium 4・Low 8）。判定 blocked→conditional の条件付き |
| `20-詳細テストケース_ws2.md` | ws2-testcases | 164 ケース（P1 44）。画面 17×通知 12×19 ステップ×異常系 38 行の対応表。T-3 に無いもの 139 件。未定 22 行 |
| `30-質問棚卸し_ws1.md` | ws1-manual | 質問 68 件を 1 表に統合、開始ブロック 4 件、W-01〜13 の書き換え完成文、定例で聞く順 |
| `31-LINE_LIFF設定手順_ws1.md` | ws1-manual | 設定項目 5 系統・開始前チェック 18・トラブルシュート 14・運用手順 4（実測 8/19 14 時台） |
| `41-panel-先方目線_haiku.md` | haiku | 分からない語 15・答えられない質問 13・不安表現 10・不足 5・成功基準なし |
| `42-panel-利用者目線_haiku.md` | haiku | 40 件（Critical 3）。Day0 同意ボタン／Day1 通知期待／痛み時の見通し。※一部はスプシの古い記述由来（コード実態は ws3・43 で補正） |
| `43-panel-資料間矛盾_sonnet.md` | sonnet | 38 件（High 7）。ID 日付化・実装状況の食い違い・開始日・稼働計画未反映 |
| `44-panel-安全_非機能_法務_opus.md` | opus | ×35／△31／○18。条件付き開放可。開始前必須 13 件（3.5〜4.5h） |
| `45-panel-工程表_開始判定_opus.md` | opus | 案 A 条件付き可。クリティカルパス 5・期限超過 10・稼働超過見込み +8.75〜11.25h |
| `46-panel-見積担当目線_sonnet.md` | sonnet | 4 枚は条件付き可。J 列定義・本番候補 12 行・NFR 6 行書き直し |

## 9. 次アクション（hq 案）

| 順 | 誰 | 何を | 目安 |
|---|---|---|---|
| 1 | naru | LIFF エンドポイント URL を `/` に更新（§3） | 1 分 |
| 2 | naru | 定例で §4 の 5 点 | 15:00 |
| 3 | hq | 判断 1・5 の結論を受けてコード修正（N-03 or 文言／alert 失敗表示／hearing ガード／flow=step 削除） | 1h |
| 4 | hq | 判断 7 の承認後、§7 をスプシへ反映（ID 列のテキスト化含む） | 1h |
| 5 | naru+hq | W-02 確定→`POC_END_DATE` 設定、`TRAINER_LINE_USER_ID` 方針確定 | 5 分 |
| 6 | hq | 20 番のテストケースのうち自動化可能な P1（vitest/curl）を追加、📱 項目は naru の実機チェックリスト化 | 2h |
| 7 | naru | 運用手順書 X-8（31 番を土台に）・規約追記（I-11）・病名言い換え（I-10・枝光様確認） | 2〜3h |
