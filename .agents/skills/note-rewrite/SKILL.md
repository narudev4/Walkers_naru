---
description: note記事を切り口だけ変えてリライト（山口鳳汰さん流ワークフロー）
---

# note記事リライト（山口流）

トリガー: 「note記事リライト」「Codex版に書き換え」「note切り口リライト」「note 山口流」

## 概要

既存のnote記事の**切り口だけを変えて別記事として量産**するワークフロー。
山口鳳汰さんがレクチャーした手順（録画: `output/manuals/note-yamaguchi/`）をスキル化したもの。

新規執筆ではなく**リライト前提**。マーケ的に「AI ◯◯」は競合が多くて刺さらないので、「Codex ◯◯」のように切り口だけ変えると一気に刺さる、という戦略。

## 戦略的意図

- マーケティングは**切り口の違い**で伸びが全然変わる
- 既存記事をベースにすることで品質を担保しながら量産可能
- noteは「自分の主張を出す場」なので**だである調**で書く（通常のコーポレート記事はですます調）

## ワークフロー全体像

```
[元記事 (note URL or 本文)]
    ↓
[切り口決定: 例「AI」→「Codex」]
    ↓
[Phase 1] ヒアリング & 標準パラメータ確認
    ↓
[Phase 2] 初稿生成（Markdown）
    ↓
[Phase 3] コメント指示 ⇄ 修正のループ（山口流の核）
    ↓
[Phase 4] note貼付用最終版を出力（人間最終調整用）
```

## 実行フロー

### Phase 1: ヒアリング

#### ヒアリング原則（重要）

- **「足りない情報だけ」を、1メッセージ1質問で聞く**。標準値が決まっている項目は確認しない。
- **必須項目** が未確定なら、その質問だけして待つ。複数同時に聞かない。
- **任意項目** はユーザー側から指定があった場合のみ受け取る。Codexから聞き返さない。
- **発話から自明に読み取れる項目は暫定確定して進める**（例: ユーザーが「Codex版に書き換えて」と言ったら切り口=Codexと暫定確定。再確認しない）。

#### 必須項目を聞く順番

1. **元記事** （これが無いと何も始まらない）
2. **切り口** （元記事を読んだ直後 or 発話から自明なら省略）
3. **CTA誘導先** （初稿を生成する直前。元記事を読んでから聞いた方がユーザーも答えやすい）

複数同時に聞かない。各ターンで1つだけ。

#### 項目一覧

| 項目 | 区分 | 標準値（未指定時の挙動） | 確認質問（必須項目のみ） |
|------|------|----------|---------|
| 元記事 | **必須** | なし | 「元記事のURL/ファイルパス/本文をください」 |
| 切り口 | **必須** | なし | 「どの切り口に置き換えますか？（例: AI→Codex）」 |
| 自社CTA動線 | **必須** | なし | 「CTAは何に誘導しますか？（例: Codex導入支援、URLがあれば一緒に）」 |
| 文体 | 任意 | **だである調**で進める | 聞かない |
| トンマナ | 任意 | **非エンジニア向け**で進める | 聞かない |
| 結論方向性 | 任意 | **元記事を踏襲**して進める | 聞かない（ユーザーから指示があれば上書き） |
| 戻し数 | 任意 | **聞かない**。ユーザー指定があればフロントマターに記録 | 聞かない |

> **NOTE**: 「戻し数3〜5」は録画で言及されたが意味が確定していない（リライト密度？AI往復回数？）。確定するまで Codex 側からは聞かない。

#### CTA URLが未指定の場合

CTA誘導先は聞くが、**URL自体が未指定**の場合は本文中に `[#]` プレースホルダーで残し、最終版でユーザーに差し替えてもらう。Codexが勝手に URL を捏造しない。

**Markdownリンクの記法:**
```markdown
[Codex導入支援に相談する]([#])
```
表示テキストの後ろのカッコの中に `[#]` を入れる（リンク先 URL 部分が `[#]`）。

#### draft の上書きポリシー

**Phase 2 の初稿生成時も Phase 3 のループ中も**、毎回**同じ draft ファイルパスに上書き保存**する。過去の履歴は残さない（会話ログに残るので十分）。同名ファイルが既に存在する場合は**確認なしで上書きしてよい**（既存内容が要件を満たしていそうに見えても、必ず再 Write する。ユーザーから見て「リライトを実行した」ことが明示されないと混乱するため）。

#### 元記事の受け取り方

ユーザーから渡された形式に応じて読み込む。**形式が判定できないときは聞き返す。**

| 渡され方 | 読み込み方 |
|---------|----------|
| URL（例: `https://note.com/...`） | `WebFetch` で本文取得 |
| ファイルパス（例: `output/articles/...md`） | `Read` ツール |
| 本文ベタ貼り | そのまま使う |
| 記事タイトルだけ | `output/articles/` を `Glob` で検索 → 候補が複数あればユーザー確認 |

### Phase 2: 初稿生成

上記で読み込んだ元記事を素材に、切り口を置換した初稿を Markdown で出力する。

> **判断軸チェック（重要）**: 出力前に **元記事の各章/論点が新切り口で成立するか毎章確認**。Codex 切り口で前提が変わって成立しなくなる論点（例: 「チーム作りが大変」→ Codex で1人で回せるなら論点が消える）は **章ごと削除/書き換え**、本文・末尾ステップの残響も削除。詳細な判断軸は `.Codex/skills/note-rewrite/yamaguchi-style.md` 参照。

**出力ルール:**

1. **フロントマター** — **draft の段階から付ける**（最終版で初出ではない）。ループ中も維持。
2. **タイトル** — 切り口を反映した新タイトル。元記事のフレーム（「終了する◯◯ vs 生き残る◯◯」等）は流用OK
3. **見出し階層**
   - H2はキャッチーな表現に
   - H2配下に**3〜4項目を並列で並べる場合は、それぞれをH3に上げる**（目次が綺麗になる・YouTube流用しやすい）
4. **文体** — だである調を徹底
5. **CTA動線** — 記事中に最低1つ、自社サービスへの導線を入れる。URL未指定なら `[#]` プレースホルダー
6. **出典の整理** — 不要な点記号・装飾は載せない
7. **ハッシュタグ・アイキャッチ案は draft では付けない**（Phase 4 で確定値を META に記載）

**保存先:** `output/articles/note/draft/YYYY-MM-DD_[テーマ].md`

**保存後 — スプシ同期（必須）:** ドラフトを Write した直後に **必ず** スプシ `マーケ` タブの該当 Row を更新する。**C列（新noteタイトル）にドラフトのタイトル（テキスト文字列のみ、note URL ハイパーリンクは naru 手動）**、D列（note進捗）を「作成中」、K列（ローカルファイル）に保存パス、J列（メモ）にリライト方針を記入。E〜I（他チャネル）は絶対に触らない。詳細手順は `.Codex/skills/note-rewrite/sheet-sync.md` 参照（スキップ禁止 = ファイルWriteだけで完了報告しない）。

### Phase 3: コメント指示 ⇄ 修正のループ（核となる工程）

山口流の最大のキモ。**ユーザーが修正したい箇所にコメントを埋め込んで返す → Codexが反映 → 繰り返す**。

#### コメント記法

ユーザーは Markdown 上で `<!-- -->` を使って Codex への指示を埋め込む。
（録画では Ulysses のコメント機能を使っていたが、Codex から見れば Markdown のコメントとして等価。）

#### よくある指示パターン（録画より抽出）

| パターン | 指示例 | Codexの対応 |
|---------|--------|------------|
| 装飾削除 | `<!-- この2つの点いらん。消して。今後も使わ -->` | 該当の点・記号を削除し、以降も使わない |
| 見出し階層調整 | `<!-- この4つの内容は見出し3として執筆 -->` | 並列項目をH3に揃える |
| H2キャッチー化 | `<!-- もっとキャッチーに -->` | H2を読者の手が止まる表現に書き直す |
| CTA挿入 | `<!-- CodexのCTA動線をつくって -->` | 該当箇所に自社誘導CTAを挿入 |
| 太字 | `<!-- これ太字やな -->` | 該当部分を太字化 |
| 構成変更 | `<!-- ここでタスクロップは実際に触ってみてもらうの3つの内容を見出し3として執筆 -->` | 指定構成で書き直す |

#### Codexの動作

ユーザーが**コメント入りMarkdown全文**を貼ってきたら:

1. すべての `<!-- -->` を読む
2. 各コメントの指示を反映
3. **コメント自体は削除して**修正版MDを返す
4. 「ここを変えました」とサマリも合わせて返す
5. ユーザーがさらにコメントを追加して投げてきたら、また反映

このループを **山口さんが「OK」と言うレベルまで** 繰り返す。

### Phase 4: note貼付用最終版を出力

**最終版への移行は明示シグナルが出てから**。Codex側で勝手に「これで完成だろう」と判断しない。

最終版発火シグナル（いずれか）:
- ユーザーから「OK」「完成」「最終版にして」「note用に出して」等の明示指示
- ユーザーから空コメント（変更なし）が返ってきた場合 → 「最終版にしますか？」と確認

シグナルが来るまでは Phase 3 のループを継続し、毎回 `output/articles/note/draft/` に上書き保存する。

シグナル発火後、**最終版**として以下のパスに保存する:

**保存先:** `output/articles/note/YYYY-MM-DD_[テーマ].md`（draft/ を含まない）

**保存後 — スプシ同期（必須）:** 最終版を Write した直後に **必ず** スプシ `マーケ` タブの該当 Row を更新する。C列（新noteタイトル）に最終版タイトル（Phase 2 で書いた値から変更があれば書き換え、テキスト文字列のみ）、K列（ローカルファイル）を最終版パス（draft/ を外す）に、J列（メモ）に FB 反映状況を記入。**D列（note進捗）は「作成中」のまま維持**（done への遷移は naru が note 投稿完了後に手動）。E〜I は絶対に触らない。詳細手順は `.Codex/skills/note-rewrite/sheet-sync.md` 参照（ファイル Write だけで完了報告しない）。

#### 最終版テンプレート（確定版）

参照: `output/articles/note/note-publish-checklist.md`（note投稿時のチェックリスト・naru 手動運用）

完成版MDは以下の構造で出力する。**執筆者カード / 運営会社カード / 目次の3種の貼付位置を1行マーカーで明示**し、note エディタ貼付後にすぐ目印として見つけられる形式にする。

> **重要**: 下記の `━━━ 📌 ... ━━━` は3種のマーカー（執筆者カード貼付位置 / 運営会社カード貼付位置 / 目次貼付位置）。naru が本文を `pbcopy` → note エディタに Cmd+V した後、UX版過去記事（[n46a5bfc33c96](https://note.com/app_dev_lab/n/n46a5bfc33c96)）から該当カードをコピペし、目次マーカー位置では note エディタの「+」メニューから目次を自動生成挿入するための「貼付位置の目印」。note エディタ上で罫線+絵文字 1 行が本文中に埋もれず必ず目立つので、貼付位置で迷わない。naru が貼付後にマーカー行（3 つとも）を削除する運用。

```markdown
---
（フロントマター）
---

# [タイトル]

[冒頭リード文（だである調・3〜5行）]

━━━ 📌 執筆者カード貼付位置 ━━━

━━━ 📌 運営会社カード貼付位置 ━━━

━━━ 📌 目次貼付位置 ━━━

[本文（H2/H3階層・だである調・CTA動線含む）]

アプリ開発研究所では累計300件以上の開発・制作、200社以上の企業様を支援してきましたが、Codex時代だからこそ[テーマ]が事業成否の最大ポイントだと考えています。

「Codexで何を作ればいいか分からない」「作ってみたが、どうも刺さらない」とお悩みの企業さまに、企画立案からCodexでの開発、リリース後のUX磨き込みまで、一気通貫でご支援しています。

無料で相談する＞＞

開発支援サービスの概要はこちら＞＞
```

> **重要**: CTA は **「無料で相談する＞＞」「開発支援サービスの概要はこちら＞＞」の 2 行プレーンテキスト**で出す。Markdown リンク `[...]([#])` ではない。URL は note エディタで貼り付け側で設定する運用。`---` の区切り線も挟まない（UX版 = 鳳汰OK実物に揃える）。

#### 最終版に含めるメタ情報（本文末尾の `<!-- META -->` ブロックに記載）

```markdown
<!-- META（投稿時の参考情報・naru が note-publish-checklist.md を見ながら手動投稿）
ハッシュタグ: #Codex #AI駆動開発
記事タイプ: 無料
マガジン: あとで読む（アプリ開発研究所）
アイキャッチ案: [タイトル文字を入れた、シンプルで見やすい画像（AI生成OK）]
-->
```

#### 引き渡し（重要）

完成版MDは naru が手動で note に投稿する。**このスキル（note-rewrite）では投稿しない**。投稿手順は `output/articles/note/note-publish-checklist.md` 参照（旧 note-publish スキルは 2026-05-13 に廃止し静的チェックリストに置き換え）。

ユーザーへの最終メッセージ:

> 完成版を `output/articles/note/YYYY-MM-DD_[テーマ].md` に保存しました。
> noteへの投稿は `output/articles/note/note-publish-checklist.md` を見ながら naru 手動で行ってください（AI は関与しません）。

## YAMLフロントマター

**draft 段階から付ける**（Phase 2 の出力ルール参照）。最終版でもそのまま維持し、フェーズ移行時に `status` を書き換える。

```yaml
---
title: [noteタイトル]
original_url: [元記事のnote URL（https://...）または ファイルパス（output/articles/...md）。どちらでもOK、形式そのまま記入]
angle: [切り口（例: Codex）]
tone: だである調
target: 非エンジニア向け
cta: [CTA誘導先]
status: draft | final | published
date: YYYY-MM-DD
platform: note
---
```

`status` の遷移:
- `draft` — Phase 2 初稿生成 / Phase 3 修正ループ中（`output/articles/note/draft/` 配下）
- `final` — Phase 4 最終版生成完了・ローカル確定（`output/articles/note/` 直下、note 投稿前）
- `published` — naru が note 投稿完了後に手動で書き換え（投稿URL `note_id` をフロントマターに追記）

スプシ `マーケ` タブの D:note進捗との対応:
- `draft`（Phase 2/3） ↔ 「作成中」
- `final`（Phase 4 完了、note 投稿前） ↔ 「作成中」（維持。マーケタブ的に draft と final は同じ「作成中」、区別は K列のパスで）
- `published`（note 投稿後） ↔ 「done」（naru が note 投稿完了後に手動遷移）

## 公開前チェックリスト

- [ ] 切り口が指定どおり置換されている（旧切り口の用語が残っていない）
- [ ] **だである調**で統一されている
- [ ] H2がキャッチーで、並列項目はH3で揃っている
- [ ] **CTA動線**が記事内に最低1つある
- [ ] 出典・装飾の冗長な点記号が削除されている
- [ ] コメント `<!-- -->` がすべて削除されている（最終版本文。ただし 3 種の `━━━ 📌 ... ━━━` 貼付位置マーカー（執筆者カード / 運営会社カード / 目次）と `<!-- META ... -->` 引き渡し情報は残す）
- [ ] ハッシュタグに `#Codex` `#AI駆動開発` が META に含まれている
- [ ] 末尾CTA定型（Codex開発支援への誘導）が入っている
- [ ] フロントマターが正しい
- [ ] 投稿は `note-publish-checklist.md` を見ながら naru 手動で行う旨を伝えた
- [ ] **スプシ `マーケ` タブの該当 Row が更新されている**（draft保存後 = **C列に新タイトル**＋ D「作成中」+ K列にdraftパス + J列にメモ、最終版保存後 = C列にタイトル（Phase 2 から変更あれば更新）+ K列に最終版パス + J列にFB反映状況、D列は「作成中」維持）。**E〜I（他チャネル）は触っていない**。`sheet-sync.md` 参照

## 関連スキル

- `note-repost` — Walkersメディア記事を note/Zenn に転載（対象選定・重複回避・iCatch処理）
- `repost-note` — 自社メディア記事をnote向けに転載リライト（山口流ではない一般版）
- `write-article` — 新規記事の0→1執筆
- `title-gen` — キャッチーなタイトル案生成

## 参照ドキュメント

- 録画原典: `output/manuals/note-yamaguchi/video.mp4`
- 文字起こし: `output/manuals/note-yamaguchi/transcript.txt`
- マニュアル: `output/manuals/note-yamaguchi/manual.md`

## 未確定事項（要すり合わせ）

このスキルは山口さんとのすり合わせで継続更新する。確定済みのテンプレ群（著者情報・運営会社情報・冒頭/末尾CTA・ハッシュタグ・マガジン・バナー扱い）は本文中の Phase 4 テンプレートに反映済み。残る未確定は以下のみ。

| 項目 | 状態 |
|------|------|
| 「戻し数3〜5」の正確な意味 | 暫定: リライト密度。要確認 |
| Codex開発支援サービスの正式URL | 未確定。当面 `[#]` プレースホルダーで残し、note 投稿時に naru 手動で差し替え（note-publish-checklist.md 参照） |
| 元記事のマスターURL一覧 | 未取得 |

更新は `.Codex/skills/note-rewrite/SKILL.md` に直接反映する。フィードバックの履歴は `00_context/memories/yamaguchi-feedback-log.md`、判断軸の要約は `.Codex/skills/note-rewrite/yamaguchi-style.md` を参照。

---

## 鳳汰スタイルガイド

→ `.Codex/skills/note-rewrite/yamaguchi-style.md` を参照（判断軸・口癖・Good/Bad 例・FB ログへのリンク）

Phase 2 初稿生成 / Phase 3 修正ループ / Phase 4 最終版で判断軸を参照する。**特に Phase 2 では「切り口で前提が変わる論点を毎章チェック」**（yamaguchi-style.md の「切り口リライト時の論点再評価」項参照）。

## フィードバックループ

### 指示の曖昧さチェック（自動）

SKILL.md 編集時に empirical-prompt-tuning が自動発火（hook設定済み）。
subagent が指示を読んで詰まる箇所を検出 → 修正。

### 成果物の品質チェック（手動）

1記事ごとに山口さんから FB 受領 → 生のFB履歴を `00_context/memories/yamaguchi-feedback-log.md` に追記、判断軸の要約を `yamaguchi-style.md` に反映（**1FBごとに即追記、閾値なし**）。
ゴールは「鳳汰さんを呼ばずに鳳汰さんレベルの成果物が出せる状態」。
